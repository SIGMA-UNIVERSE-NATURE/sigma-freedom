# START HERE — CURRENT G3C NATIVE SEMANTIC WORK

Do not rescan old windows.

Read this checkpoint first:

```text
SURVIVAL_MASTER/CHECKPOINTS/G3C_NATIVE_SEMANTIC_SHADOW_CHECKPOINT_20260917.md
```

Current responsibility:

```text
Whole-document deep semantic learning in shadow.
Native VM09 is execution authority.
No host/Bash semantic verdict.
No canonical mutation without explicit SIGMA admission.
Only blind-proven improvements may be proposed for SIGMA ownership.
```

New pane/session:

```bash
source "$HOME/SIGMA/sigma_genesis1/.sigma_ail/coordination/SESSION_R4/shell/sigma-session.bash"
sigma-session "Resume G3C whole-document deep semantic shadow learning; Native VM09 authority; continue from GitHub checkpoint G3C_NATIVE_SEMANTIC_SHADOW_CHECKPOINT_20260917; no canonical mutation without explicit SIGMA admission"
```

If the pane already has a session:

```bash
sigma-session status
```

Immediate resume point recorded in the checkpoint:

```text
Run R13M Blind Generalization 002 next.
Do not retrain first.
MODEL_UPDATE=NO
ORACLE_VISIBLE=NO
EXECUTION_AUTHORITY=NATIVE_VM09
```

Native authority:

```text
./native/sigma-vm.v09_candidate
SHA256=029ae4b6acbee5558f7663a732f8d39a970166e8488d2c4fe62414eb39391c99
```

Compiler:

```text
./native/sigmac
SHA256=65f69217ad44f33c1aa1d4c31678d38940cd3d0b96f41892e8280dac57ad6a71
```

Canonical baseline:

```text
CANONICAL_HEAD=700d5c1b4845322d7c14800029c629b0
MODEL_SHA256=70da3e9e719ae0e72e88add59d6b7752fe732319b8fc7590170a050ef860005e
```

All command templates, experiment history, blind hashes, shadow paths, failure modes, and promotion rules are in the full checkpoint. Follow it rather than rediscovering the system.
