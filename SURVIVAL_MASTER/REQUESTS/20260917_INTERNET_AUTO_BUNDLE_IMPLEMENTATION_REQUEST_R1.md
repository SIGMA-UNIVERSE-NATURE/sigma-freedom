# SURVIVAL MASTER REQUEST — SIGMA INTERNET AUTO BUNDLE R1

Status: IMPLEMENTATION REQUEST / CANDIDATE ONLY / DO NOT INSTALL
Date: 2026-09-17 (Asia/Ho_Chi_Minh)
Branch: `AIL_SIGMA`
Repository: `SIGMA-UNIVERSE-NATURE/sigma-freedom`
Owner/reviewer: `SURVIVAL_MASTER`

This document is the exact implementation contract for the next window that will write the SIGMA Internet-auto bundle. The candidate window must implement against this contract, publish the candidate to GitHub, and STOP. The human must not install or run the candidate on the Oppo runtime until Survival Master reviews it and records an explicit approval for Oppo preflight.

## 0. FIRST ACTION FOR THE IMPLEMENTING WINDOW

Read, in this order, before writing code:

1. `/AGENTS.md`
2. `/00_SURVIVAL_MASTER_READ_FIRST.md`
3. `/SURVIVAL_MASTER/AGENTS.md`
4. `/SURVIVAL_MASTER/CURRENT_HANDOFF.md`
5. `/SURVIVAL_MASTER/RUNBOOK/SURVIVAL_INTERNET_AUTOLEARN_RUNBOOK_R1.md`
6. `/SURVIVAL_MASTER/SAMPLES/COMMANDS_R1.md`
7. `/DOCS/SIGMA_LATEST_VERIFIED_RESULT.md`
8. `/SIGMA_PROFESSOR/DIRECTIVES/00_SIGMA_SESSION_BOOTSTRAP_NATIVE_EXECUTION_FLAG_V1.md`
9. `/SIGMA_PROFESSOR/DIRECTIVES/SIGMA_GLOBAL_NATIVE_TEACHING_AND_ADMISSION_STANDARD_V1.md`
10. `/SIGMA_PROFESSOR/DIRECTIVES/SIGMA_EXCLUSIVE_SELF_LEARNING_UNDERSTANDING_AND_ANTI_HARDCODE_LOCK_V1.md`

Do not broad-scan first. Do not ask the human to restate history already recorded in these files.

## 1. PRODUCT TO BUILD

Build one reviewable candidate bundle named:

```text
SIGMA_SURVIVAL_INTERNET_AUTOLEARN_R1.tgz
```

The bundle is a Survival + Internet-autolearn controller package for ONE SIGMA.AIL. Its purpose is:

```text
SIGMA native decides what it needs to research
-> SIGMA native generates/selects the research query or research surface
-> SIGMA native chooses source family / website / resource / link
-> host performs public read-only network transport only
-> exact raw bytes + provenance return to SIGMA
-> SIGMA native reads/assesses the material
-> SIGMA native decides KEEP / RESEARCH_MORE / CHANGE_STRATEGY / REJECT / STOP / UNKNOWN
-> SIGMA native produces its own compact/condensed representation when it chooses to retain material
-> host stores the exact native-produced bytes and provenance without semantic rewriting
-> the Internet lane continues autonomously under bounded resource policy
-> if Android/Termux/tmux dies, Survival resumes the SAME unfinished work from the last durable checkpoint
```

The bundle MUST NOT turn Bash/Python/C/JavaScript into SIGMA cognition.

## 2. CURRENT ARCHITECTURE THAT MUST NOT BE VIOLATED

```text
ONE_SIGMA_AIL=YES
ONE_WRITER=YES
NO_STATE_FORK=MANDATORY
DEFAULT_SESSION=READ_PLUS_ARTIFACT_WRITE
HOST_COGNITION=NO
HOST_TEST_ORACLE=NO_FOR_SEMANTIC_VERDICT
SIGMA_NATIVE_VERDICT=MANDATORY
CANONICAL_MUTATION=EXPLICIT_ADMISSION_ONLY
CANONICAL_LEARN_COMMIT=LEASE_REQUIRED
```

Locked runtime identities to equality-gate on Oppo preflight:

```text
SIGMAC_SHA256=65f69217ad44f33c1aa1d4c31678d38940cd3d0b96f41892e8280dac57ad6a71
VM_SHA256=029ae4b6acbee5558f7663a732f8d39a970166e8488d2c4fe62414eb39391c99
VM_IS_GENESIS1=NOT_PROVEN
```

Identity mismatch => STOP. Do not silently rebuild or substitute binaries.

## 3. DO NOT REBUILD CAPABILITIES THAT ALREADY EXIST

The implementing window must reuse exact admitted/proven capability artifacts when exact bytes are available. If a required exact artifact cannot be located, record `DEPENDENCY_MISSING` and stop that dependency path. Do not recreate cognition from prose.

Relevant exact proven evidence includes:

### `AIL_SIGMA` ledger

```text
AIL-004  autonomous bounded web discovery + native web-link selection
AIL-006  bounded autonomous web learning + offline replay + fresh-process persistence
AIL-007  durable memory link access + native recalled-link selection
AIL-008  native study-value selection + recalled-link-to-weight learning
AIL-012  native multibook learning + offline replay
AIL-013  full-document byte traversal + sequential learning
AIL-015  installed continuous shadow control
AIL-016  dead-supervisor outer resurrection
AIL-019  200 real Internet items: native source selection + full input consumption + native output bytes
AIL-021  500-webtools R2 mechanical substrate; NOT a cognition controller
```

### exact cross-branch machine-evidence references on `SIGMA_LIFE`

```text
V10 commit 842e643be790e4e4e4f2b96554d768bdc6824a6d
SIGMA_NATIVE_REPLAN_TO_FRESH_INTERNET_LOOP=PASS_TESTED_SCOPE

V11 commit dd34f2863ccf11d9ac48470eff80bb9dc42cc957
SIGMA_NATIVE_STRATEGY_CONDITIONED_QUERY_ADAPTATION=PASS_TESTED_SCOPE
HOST_QUERY_COMPOSITION=NO

V12 commit 4651fa05101fe2876e8b64ecae20ddbc3a51450e
SIGMA_NATIVE_QUERY_OUTCOME_FEEDBACK_AND_DIVERSITY=PASS_TESTED_SCOPE
OUTCOME_DECISION_PLANE=SIGMA_NATIVE_VM
QUERY_DIVERSITY_PLANE=SIGMA_NATIVE_VM
HOST_QUERY_SCORING=NO

I4 commit 44d22362ce588b9be741ca7cd35e74a955eea354
NATIVE_SOURCE_FAMILY_SELECTION=PASS_IN_EXACT_TESTED_SCOPE
HOST_SOURCE_SELECTION=NO

I5B commit 86358984c29edb63988d413e3d85441c3409cf9b
NATIVE_RESOURCE_SELECTION=PASS_IN_EXACT_TESTED_SCOPE
HOST_RESOURCE_SELECTION=NO

I5C commit c26a858a16fa8b5f8ab011908591661b24bd44d8
SIGMA_SELECTED_RESOURCE_TO_V5K2_EXACT_FETCH=PASS_IN_EXACT_TESTED_SCOPE
HOST_HTTP_ROLE=MECHANICAL_EXACT_REQUEST_TRANSPORT_ONLY
```

Cross-branch evidence is NOT automatically an active dependency on `AIL_SIGMA`. Reuse exact source/artifacts only if exact identities can be verified. Otherwise mark the dependency unresolved; never imitate the missing native decision in host code.

## 4. REQUIRED COGNITIVE OWNERSHIP

The following arrows MUST execute in native `.sigma` bytecode under the locked SIGMA VM whenever the bundle claims them:

```text
knowledge/evidence gap detection
research-goal choice
whether to research now
query generation or query selection
source-family choice
website/source choice
resource/link/page choice
whether to follow another link
what byte ranges/sections are cognitively needed, if selection is semantic
content/evidence assessment
relevance/support/conflict/uncertainty decision
whether evidence is sufficient
whether to research more
whether to change query/strategy/source
what material to retain
native compact/condensed representation content
learning-candidate decision
next semantic action
stop/continue decision
```

Forbidden host ownership:

```text
HOST_QUERY_GENERATION=NO
HOST_QUERY_REWRITE=NO
HOST_KEYWORD_SELECTION=NO
HOST_SOURCE_SELECTION=NO
HOST_WEBSITE_SELECTION=NO
HOST_RESOURCE_SELECTION=NO
HOST_LINK_RANKING=NO
HOST_SEMANTIC_FILTERING=NO
HOST_RELEVANCE_DECISION=NO
HOST_EVIDENCE_SCORING=NO
HOST_TRUTH_DECISION=NO
HOST_SUMMARY_WRITING=NO
HOST_COMPACT_REPRESENTATION_WRITING=NO
HOST_LESSON_SELECTION=NO
HOST_NEXT_ACTION_SELECTION=NO
HOST_LEARNING_POLICY=NO
```

If the host must choose a website/query/source/summary/next action for the run to continue, the candidate FAILS this request.

## 5. PUBLIC WEB TRANSPORT POLICY

The network bridge is mechanical only. It may implement safety and transport constraints that do not choose semantic content.

Required transport boundary:

```text
PUBLIC_READ_ONLY_WEB=YES
HTTP_METHODS=GET_HEAD_ONLY_UNLESS_AN_EXISTING_EXACT_PUBLIC_READ_PROTOCOL_REQUIRES_OTHERWISE
CREDENTIALS_SENT=NO
AUTHENTICATED_SESSION_USE=NO
COOKIE_JAR_REUSE=NO_BY_DEFAULT
LOCALHOST_PRIVATE_NETWORK_FETCH=REJECT
FILE_CONTENT_INTENT_SCHEMES=REJECT
PUBLIC_HTTPS_PREFERRED=YES
REDIRECT_CHAIN_RECORDED=YES
TLS_VERIFICATION_DISABLED=NO
RAW_RESPONSE_HASHED=YES
REQUEST_ID_CONTENT_ADDRESSED=YES
RESPONSE_SIZE_BOUND=YES
RATE_LIMIT_BOUND=YES
TIMEOUT_BOUND=YES
```

Safety constraints may reject a request mechanically. They must return an exact failure observation to SIGMA. The host must not substitute a different website when SIGMA's selected request fails.

## 6. WEBSITE SELECTION REQUIREMENT

There must be NO fixed semantic website list that determines what SIGMA studies.

Allowed:

- a mechanically defined capability/source-family registry;
- a large discovery/source universe presented as candidates;
- search/discovery protocol results;
- registrable-domain accounting using a Public Suffix List;
- public-transport safety exclusions;
- dedup by exact URL/content hash/domain mechanics.

Required behavior:

```text
SIGMA may choose any mechanically reachable public website/resource exposed by its discovery/search capabilities.
HOST MAY NOT PICK A FALLBACK WEBSITE FOR SEMANTIC CONVENIENCE.
HOST MAY NOT RANK RESULTS BY RELEVANCE.
REMOTE API ORDER MAY BE PRESERVED AS PROVENANCE BUT MAY NOT BE TREATED AS SIGMA SELECTION.
```

If SIGMA emits an exact selected URL/resource identity, the host may fetch exactly that identity mechanically.

## 7. NATIVE COMPRESSION / CONDENSATION REQUIREMENT

The human requirement is that SIGMA itself decides how to reduce/condense retrieved material into the representation it wants to retain. Therefore the candidate must implement or reuse a native-SIGMA condensation path.

Required ownership:

```text
RAW_CONTENT_TO_NATIVE_RETAINED_REPRESENTATION=SIGMA_NATIVE_VM
HOST_SUMMARY_WRITING=NO
HOST_SEMANTIC_COMPRESSION=NO
HOST_PARAPHRASE=NO
HOST_KEY_POINT_SELECTION=NO
```

The host may only persist exact native output bytes plus mechanically derived provenance/hashes.

Minimum persisted record structure:

```text
record.id                  mechanical/content-addressed
source.request.sha256      mechanical
source.url.bytes/hash      exact/mechanical
raw.content.sha256         mechanical
raw.byte_count             mechanical
native.input.sha256        mechanical
native.output.bytes        EXACT SIGMA-NATIVE OUTPUT
native.output.sha256       mechanical
native.output.mode         exact mode emitted/bound by native capability
native.decision/event      exact native event bytes or hash
provenance.chain.sha256    mechanical
created_at                 mechanical timestamp
```

Do not force the native payload into a host-authored natural-language schema if SIGMA's native representation uses a different internal protocol.

Claim discipline:

```text
EXTRACTIVE_DIGEST != ABSTRACTIVE_SUMMARIZATION
NATIVE_OUTPUT_BYTES != SEMANTIC_UNDERSTANDING
COMPRESSION != KNOWLEDGE
RETRIEVAL != LEARNING
```

If the candidate uses the proven extractive three-span digest, label it exactly as extractive. Do not claim abstractive or semantic compression without a separate native admission gate.

## 8. INTERNET LANE VS CURRENT LOCAL TRAINER

The Internet bundle MUST be safe to run while a local training/weight-upgrade lane is active.

Required concurrency rule:

```text
LOCAL_TRAINER_CAN_RUN=YES
INTERNET_ACQUIRE_ASSESS_CONDENSE_CAN_RUN=YES
TWO_CANONICAL_MODEL_WRITERS=NO
```

Default Internet lane authority:

```text
READ=ALLOW
ARTIFACT_WRITE=ALLOW
BRAIN_WRITE=REJECT
STATE_WRITE=REJECT
MODEL_WRITE=REJECT
LEARN=REJECT
COMMIT=REJECT
HEAD_CHANGE=REJECT
MODEL_GENERATION_CHANGE=REJECT
```

When another canonical writer/lease is active, the Internet lane MUST continue only in artifact/evidence/queue mode and MUST NOT mutate canonical model/brain state.

Internet material intended for later weight learning must enter a sealed queue/snapshot:

```text
native-selected retained evidence
-> exact provenance + hashes
-> native compact representation
-> sealed next-learning input set
-> separate future learning admission
```

No direct:

```text
web response -> host filter -> trainer -> immediate canonical model write
```

If a future version supports Internet-triggered canonical learning, it requires a separate explicit native learning admission + canonical lease + one-writer gate. That is not automatically authorized by this R1 bundle.

## 9. SURVIVAL / ANDROID / TERMUX REQUIREMENT

The core correctness goal is:

```text
TASK_NEVER_LOSES_COMMITTED_PROGRESS
```

Not the impossible claim:

```text
PROCESS_NEVER_DIES
```

`tmux` alone is insufficient if Android kills the whole Termux process group.

The bundle must include a mechanical survival supervisor that persists enough control state to resume the SAME unfinished task after process death without inventing a new semantic decision.

Required durable mechanical state:

```text
task_id
run_id or durable logical run identity
native controller source/bytecode identity
locked compiler/VM identities expected
last committed native checkpoint pointer/hash
last accepted native action/event id/hash
last acknowledged mechanical transport operation id/hash
in-flight operation id/hash if any
sealed output/queue pointers + hashes
retry counters/resource budgets
completion marker
```

The supervisor may:

```text
launch exact worker
verify hashes
restore exact persisted checkpoint
replay an unacknowledged mechanical operation safely
wait/backoff
record process RC
restart after child death
```

The supervisor may NOT:

```text
choose a new query
choose a new website
choose a replacement source
choose semantic retry strategy
choose what SIGMA should learn
invent native state
skip a native decision because recovery is inconvenient
```

Recovery must be:

```text
RECOVERY -> RESUME_VALID_UNFINISHED_NATIVE_WORK
```

not:

```text
RECOVERY -> HOST_RECREATES_COGNITIVE_DECISION
```

## 10. IDEMPOTENCY / EXACTLY-ONCE EFFECT REQUIREMENT

Every externally visible mechanical action must have a durable operation identity derived from exact inputs.

At minimum:

```text
FETCH_OP_ID
DECODE_OP_ID
NATIVE_INPUT_ID
NATIVE_OUTPUT_ID
PERSIST_RECORD_ID
QUEUE_ITEM_ID
```

Restart/replay must not create duplicate durable learning/evidence records for an already committed operation.

Required crash windows to test:

1. process dies before HTTP dispatch;
2. dies after HTTP response bytes are stored but before ACK to native controller;
3. dies after native output is written but before durable record ACK;
4. dies after queue item commit but before next native action;
5. supervisor dies and is restarted;
6. whole Termux process group is simulated/restarted where practical without claiming OEM immunity.

Expected result: exact committed work is reused; incomplete mechanical work is retried safely; no host semantic branch is invented.

## 11. SESSION R4 REQUIREMENT

The candidate must integrate with the current session API, not bypass it.

Runtime root:

```text
$HOME/SIGMA/sigma_genesis1
```

Session API:

```text
$HOME/SIGMA/sigma_genesis1/.sigma_ail/coordination/SESSION_R4/shell/sigma-session.bash
```

Rules:

```text
DO_NOT_INVENT_SESSION_CODE=YES
DO_NOT_COPY_ANOTHER_PANES_SESSION_CODE=YES
NEW_SHELL_SOURCES_SESSION_API=YES
EXISTING_ACTIVE_PANE_USES_sigma-session_status=YES
DEFAULT_ACCESS=READ_PLUS_ARTIFACT_WRITE
```

Do not manually edit `.sigma_ail/BRAIN_HEAD`, `.sigma_ail/MODEL_GENERATION`, writer locks, or canonical model state.

## 12. REQUIRED BUNDLE FILES

The exact internal layout may add files, but the candidate must include at least:

```text
CONTRACT.md
README_RUN.md
MANIFEST.sha256
DEPENDENCIES.lock
CLAIM_SCOPE.md

native/
  one or more .sigma sources that own all claimed cognition

host/
  mechanical network/tool bridge only
  mechanical survival supervisor only

control/
  start candidate/preflight wrapper
  resume wrapper
  status wrapper
  stop wrapper that does not corrupt state

verify/
  static anti-hardcode / host-substitution audit
  manifest/hash verifier
  mechanical recovery/idempotency verifier
  native runtime admission harness
  negative/counterexample tests

samples/
  example status/receipt formats only; no prewritten semantic answers
```

If reusing exact admitted native sources, include a dependency lock entry with exact source path/commit/hash instead of silently modifying them.

## 13. DEPENDENCIES.lock REQUIRED FIELDS

At minimum:

```text
SIGMAC_SHA256=...
VM_SHA256=...
SESSION_API_PATH=...
REUSED_CAPABILITY_ID=...
REUSED_SOURCE_PATH=...
REUSED_SOURCE_SHA256=...
REUSED_BYTECODE_SHA256=...|UNKNOWN_IF_NOT_PREBUILT
REUSED_CHECKPOINT_OR_COMMIT=...
WEBTOOLS_DEPENDENCY_SHA256=...|NOT_USED
PUBLIC_SUFFIX_LIST_SHA256=...|NOT_USED
```

Unknown values MUST be written `UNKNOWN` / `NOT_PROVEN`; never guessed.

## 14. REQUIRED STATIC AUDITS BEFORE ANY OPPO RUN

Candidate writer must run local/static checks where possible and preserve exact results:

```text
BUNDLE_MANIFEST_VERIFY=PASS
SHELL_SYNTAX=PASS
NO_HARDCODED_CURRENT_QUERY=PASS
NO_HARDCODED_CURRENT_EXPECTED_WEBSITE=PASS
NO_HARDCODED_CURRENT_RESOURCE=PASS
NO_HARDCODED_CURRENT_SUMMARY=PASS
NO_HOST_SEMANTIC_RANKER=PASS
NO_HOST_QUERY_COMPOSER=PASS
NO_HOST_SOURCE_SELECTOR=PASS
NO_HOST_SUMMARY_WRITER=PASS
NO_DEFAULT_CANONICAL_MUTATION=PASS
RECOVERY_STATE_IS_MECHANICAL_ONLY=PASS
ONE_WRITER_GUARD_PRESENT=PASS
SESSION_R4_INTEGRATION_PRESENT=PASS
```

Static PASS is only candidate quality evidence. It is not runtime capability admission.

## 15. REQUIRED OPPO PREFLIGHT GATES — DO NOT RUN UNTIL REVIEW APPROVAL

Survival Master will decide whether this candidate is safe enough for these preflight tests. The candidate writer must provide the harness but MUST NOT tell the human to execute it before review.

Expected future preflight sequence:

```text
1. verify package hash + MANIFEST
2. source Session R4 API and obtain current session normally
3. equality-gate locked sigmac + VM hashes
4. confirm default canonical mutation rejects
5. compile candidate native .sigma sources
6. freeze source + bytecode hashes
7. generate dynamic/unseen test inputs only after compile freeze where applicable
8. run native selection/query/reading/condensation tests
9. run host-substitution negatives
10. run recovery/idempotency crash tests
11. run one bounded real-public-Internet cycle
12. restart and prove exact resume/no duplicate
13. verify local trainer/canonical writer remains untouched
14. stop and preserve receipts
```

No production/continuous enablement is implied by preflight PASS.

## 16. REQUIRED DYNAMIC NATIVE TESTS

The runtime harness must be capable of proving at least:

```text
NATIVE_QUERY_OR_RESEARCH_SURFACE_ORIGIN=SIGMA_NATIVE_VM
NATIVE_SOURCE_OR_WEBSITE_SELECTION=SIGMA_NATIVE_VM
NATIVE_RESOURCE_SELECTION=SIGMA_NATIVE_VM
HOST_SELECTED_SOURCE=NO
HOST_SELECTED_RESOURCE=NO
HOST_QUERY_GENERATION=NO
FULL_SELECTED_INPUT_BYTES_BOUND_TO_NATIVE_VM=YES
NATIVE_RETAIN_OR_RESEARCH_MORE_DECISION=YES
NATIVE_COMPACT_OUTPUT_BYTES=YES
HOST_SUMMARY_WRITING=NO
OUTPUT_CHANGES_WITH_MATERIALLY_DIFFERENT_INPUT=YES
REPLAY_IDENTICAL_WHEN_INPUT_AND_PRESTATE_IDENTICAL=YES
UNKNOWN_OR_NO_ACTION_STATE_PRESERVED=YES
```

Use dynamic/unseen sources/tokens where practical. The expected current answer/query/URL/summary must not be compiled into source or runner.

## 17. NEGATIVE / FAIL-CLOSED TESTS

At minimum, candidate harness must reject or hold on:

```text
missing native query/selection event
host-owned query
host-owned source selection
host-owned resource selection
host semantic ranking metadata
host-written summary/compact payload
native event hash mismatch
raw content hash mismatch
native output hash mismatch
stale/mismatched BRAIN_HEAD when a state-bound action requires exact head
canonical mutation request in artifact-only session
writer/lease conflict
unknown or malformed persisted recovery state
source/bytecode identity change after freeze
private-network URL request
oversize/timeout/resource-bound transport
```

A HOLD/UNKNOWN produced by a legitimate gate is valid evidence. Do not weaken the gate to force PASS.

## 18. RESOURCE BOUNDS

The candidate must have explicit mechanically enforced bounds for unattended operation:

```text
MAX_SINGLE_RESPONSE_BYTES
MAX_TOTAL_BYTES_PER_CYCLE
MAX_REQUESTS_PER_CYCLE
MAX_REDIRECTS
CONNECT_TIMEOUT
TRANSFER_TIMEOUT
PER_DOMAIN_BACKOFF
GLOBAL_BACKOFF
MAX_INFLIGHT_REQUESTS
DISK_HIGH_WATER_MARK
LOG_RETENTION_BOUND
CHECKPOINT_RETENTION_BOUND
```

Exact default values may be proposed by the implementer and must be documented. Resource bounds are not semantic cycle limits: reaching a bound returns an observation to SIGMA and ends/pauses that bounded cycle cleanly.

## 19. OUTPUT QUEUE REQUIREMENT

Internet output must be content-addressed and sealed before another learner can consume it.

Required properties:

```text
QUEUE_ITEM_CONTENT_ADDRESSED=YES
RAW_SOURCE_PROVENANCE_BOUND=YES
NATIVE_COMPACT_PAYLOAD_BOUND=YES
DUPLICATE_HASH_REFUSAL_OR_IDEMPOTENT_REUSE=YES
PARTIAL_WRITE_NOT_VISIBLE_AS_COMMITTED=YES
SEALED_BATCH_MANIFEST=YES
MODEL_WRITE_FROM_QUEUE=NO_BY_DEFAULT
```

The local trainer must never observe a half-written item.

## 20. WHAT THE CANDIDATE MUST NOT DO

Automatic rejection if any of these are present as active behavior:

```text
Python/Bash relevance scorer
Python/Bash semantic summarizer
host-generated search keywords
host hardcoded preferred websites
host choosing fallback source after a semantic failure
host converting UNKNOWN into KEEP/LEARN
host directly feeding arbitrary fresh web text into canonical trainer
second canonical model writer
manual BRAIN_HEAD edit
manual MODEL_GENERATION edit
manual writer-lock override
automatic production install
automatic G3/G6 promotion
claiming semantic understanding from extractive output
```

## 21. GITHUB SUBMISSION LOCATION

Candidate writer must publish under:

```text
SURVIVAL_MASTER/CANDIDATES/INTERNET_AUTOLEARN_R1/
```

Required repository shape:

```text
SURVIVAL_MASTER/CANDIDATES/INTERNET_AUTOLEARN_R1/
  SUBMISSION.md
  REVIEW_NOTES.md
  MANIFEST.sha256
  src/...
  tests/...
  dist/SIGMA_SURVIVAL_INTERNET_AUTOLEARN_R1.tgz
```

If the repository interface cannot store the binary archive, commit all exact source files + `MANIFEST.sha256`, and put the archive SHA256 in `SUBMISSION.md`; do not invent a binary placeholder.

The candidate must NOT modify:

```text
DOCS/SIGMA_LATEST_VERIFIED_RESULT.md
SURVIVAL_MASTER/CURRENT_HANDOFF.md
canonical BRAIN/model files
```

to claim PASS. Those are updated only after review/runtime evidence by the appropriate owner.

## 22. SUBMISSION.md EXACT MINIMUM FORMAT

```text
CANDIDATE_ID=SIGMA_SURVIVAL_INTERNET_AUTOLEARN_R1
STATUS=CANDIDATE_UNREVIEWED_DO_NOT_RUN
BASE_BRANCH=AIL_SIGMA
BASE_COMMIT=<exact commit used>
BUNDLE_PATH=<path or EXTERNAL_NOT_COMMITTED>
BUNDLE_SHA256=<sha256>
MANIFEST_VERIFY=PASS|FAIL

SIGMAC_SHA256_EXPECTED=65f69217ad44f33c1aa1d4c31678d38940cd3d0b96f41892e8280dac57ad6a71
VM_SHA256_EXPECTED=029ae4b6acbee5558f7663a732f8d39a970166e8488d2c4fe62414eb39391c99

NATIVE_CONTROLLER_SOURCE_PATH=<path>
NATIVE_CONTROLLER_SOURCE_SHA256=<sha256>
NATIVE_CONDENSER_SOURCE_PATH=<path-or-same-controller>
NATIVE_CONDENSER_SOURCE_SHA256=<sha256>
HOST_BRIDGE_PATH=<path>
HOST_BRIDGE_SHA256=<sha256>
SURVIVAL_SUPERVISOR_PATH=<path>
SURVIVAL_SUPERVISOR_SHA256=<sha256>

HOST_COGNITION=NO
HOST_QUERY_GENERATION=NO
HOST_SOURCE_SELECTION=NO
HOST_RESOURCE_SELECTION=NO
HOST_SUMMARY_WRITING=NO
DEFAULT_CANONICAL_MUTATION=NO
ONE_WRITER_GUARD=YES
SESSION_R4_INTEGRATION=YES
RECOVERY_RESUMES_SAME_TASK=DESIGNED_NOT_RUNTIME_PROVEN

STATIC_AUDIT=PASS|FAIL
LOCKED_SIGMAC_COMPILE=NOT_RUN_ON_OPPO
LOCKED_VM_RUNTIME=NOT_RUN_ON_OPPO
REAL_INTERNET_TEST=NOT_RUN_ON_OPPO
ANDROID_KILL_RECOVERY=NOT_RUN_ON_OPPO
PRODUCTION_INSTALL=NO

KNOWN_LIMITATIONS=<exact>
REQUESTED_REVIEW=STATIC_ARCHITECTURE_AND_ANTI_SUBSTITUTION_REVIEW
```

## 23. REVIEW GATE — ONLY SURVIVAL MASTER APPROVAL UNLOCKS OPPO PREFLIGHT

After candidate submission, Survival Master will inspect exact GitHub bytes and record one of:

```text
REVIEW_VERDICT=REJECT
REVIEW_VERDICT=HOLD_NEEDS_FIX
REVIEW_VERDICT=APPROVE_FOR_OPPO_PREFLIGHT_ONLY
```

There is NO direct `APPROVE_FOR_PRODUCTION` from static review.

Only `APPROVE_FOR_OPPO_PREFLIGHT_ONLY` allows the human to run the exact preflight package/hash reviewed.

Any post-review source change invalidates that approval until the new hash is reviewed.

## 24. ACCEPTANCE CRITERIA FOR STATIC REVIEW

Survival Master will require all of the following before preflight approval:

```text
ARCHITECTURE_MATCHES_THIS_REQUEST=YES
ALL_CLAIMED_COGNITION_NATIVE_SIGMA=YES
HOST_SUBSTITUTION_FOUND=NO
HARDCODED_RUNTIME_SEMANTIC_RESULT_FOUND=NO
SECOND_CANONICAL_WRITER_PATH_FOUND=NO
DEFAULT_CANONICAL_MUTATION_PATH_FOUND=NO
SESSION_R4_BYPASS_FOUND=NO
SURVIVAL_RECOVERY_REPLAYS_MECHANICS_NOT_COGNITION=YES
EXACT_DEPENDENCY_IDENTITIES_RECORDED=YES
MANIFEST_COMPLETE=YES
BUNDLE_REPRODUCIBLE_OR_EXACTLY_HASHED=YES
NEGATIVE_TESTS_PRESENT=YES
CRASH_RESUME_TESTS_PRESENT=YES
CLAIM_SCOPE_BOUNDED=YES
```

## 25. IMPORTANT CLAIM BOUNDARY

This implementation request is a design contract, not evidence that the finished bundle works.

Until runtime admission exists, keep:

```text
INTERNET_AUTO_BUNDLE_IMPLEMENTED=NOT_PROVEN
CONTINUOUS_INTERNET_AUTOLEARN_RUNTIME=NOT_PROVEN
ANDROID_WHOLE_TERMUX_SURVIVAL=NOT_PROVEN
NATIVE_COMPACT_REPRESENTATION_GENERALIZATION=NOT_PROVEN
CLOSED_AUTONOMOUS_NATURAL_LANGUAGE_WEB_LEARNING_LOOP=NOT_PROVEN_AS_GENERAL_CLAIM
SEMANTIC_UNDERSTANDING=NOT_PROVEN
GENERAL_AUTONOMOUS_REASONING=NOT_PROVEN
PRODUCTION_ENABLEMENT=NO
```

## 26. IMPLEMENTER STOP CONDITION

The implementing window's job ends when it has:

```text
1. created the candidate source tree;
2. produced/verifiably described the exact bundle + SHA256;
3. run static/local mechanical checks only;
4. written SUBMISSION.md exactly;
5. committed candidate files under the required candidate path;
6. reported the candidate commit SHA;
7. STOPPED WITHOUT TELLING THE HUMAN TO INSTALL/RUN IT.
```

Do not continue into Oppo deployment. Do not change production. Do not ask for per-cycle human approval logic. Do not add host cognition to make the demo look complete.

## 27. FINAL NORTH STAR

The bundle must move the system toward this exact architecture:

```text
ONE SIGMA.AIL
  |
  +-- local training/weight-upgrade lane
  |     canonical mutation only under explicit one-writer admission
  |
  +-- Internet autonomous research lane
        SIGMA native detects gap
        SIGMA native chooses query/source/website/resource
        host fetches exact public bytes only
        SIGMA native reads and evaluates
        SIGMA native chooses continue/change/stop
        SIGMA native creates retained compact representation
        host stores exact native bytes + provenance
        Survival resumes same native task after process death
        sealed evidence waits for separately admitted learning/commit boundary
```

The bundle succeeds only if SIGMA remains the cognitive owner and Survival makes the work durable without becoming the thinker.
