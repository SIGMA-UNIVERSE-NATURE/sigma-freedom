# SURVIVAL MASTER — CURRENT HANDOFF

Status date: 2026-09-17 (Asia/Ho_Chi_Minh)
Branch: `AIL_SIGMA`
Repository: `SIGMA-UNIVERSE-NATURE/sigma-freedom`
Checkpoint base before this handoff: `1ee81a787ccb2d921fbbadf41c9a3251521812c1`

This file is the fast-resume handoff for any future window, teacher, administrator, recovery worker, or maintainer that must continue the current Survival + Internet-autolearn work without rediscovering the project from scratch.

## READ THIS FIRST

For Survival / Internet-autolearn work, read in this exact order:

1. `/AGENTS.md`
2. `SURVIVAL_MASTER/CURRENT_HANDOFF.md` — this file
3. `SURVIVAL_MASTER/RUNBOOK/SURVIVAL_INTERNET_AUTOLEARN_RUNBOOK_R1.md`
4. `SURVIVAL_MASTER/SAMPLES/COMMANDS_R1.md`
5. `DOCS/SIGMA_LATEST_VERIFIED_RESULT.md`
6. `SIGMA_PROFESSOR/DIRECTIVES/00_SIGMA_SESSION_BOOTSTRAP_NATIVE_EXECUTION_FLAG_V1.md`
7. `SIGMA_PROFESSOR/DIRECTIVES/SIGMA_GLOBAL_NATIVE_TEACHING_AND_ADMISSION_STANDARD_V1.md`
8. `SIGMA_PROFESSOR/DIRECTIVES/SIGMA_EXCLUSIVE_SELF_LEARNING_UNDERSTANDING_AND_ANTI_HARDCODE_LOCK_V1.md`
9. latest relevant immutable capability checkpoint only if a specific dependency must be inspected.

Do not broad-scan the repository before reading these files. Reuse exact proven artifacts and hashes first.

## AUTHORITY ORDER

```text
LIVE OPPO RUNTIME / MACHINE RECEIPT
> exact local artifact bytes + hashes
> immutable GitHub capability checkpoint
> this Survival Master handoff
> chat summary / memory / prose
```

GitHub is continuity documentation, not authority over current process liveness. If this file says a process was alive historically but Oppo says it is dead now, Oppo wins.

## ROLE

```text
ROLE=ADMINISTRATOR_SURVIVAL_MASTER
GENERATION_AFFILIATION=NONE
HOST_COGNITION=NO
HOST_TEST_ORACLE=NO_FOR_SEMANTIC_VERDICT
SIGMA_NATIVE_VERDICT=MANDATORY
MODEL_MUTATION_AUTHORITY=NO_BY_DEFAULT
CANONICAL_MUTATION=EXPLICIT_ADMISSION_ONLY
SURVIVAL_SCOPE=PROCESS_TASK_CHECKPOINT_RECOVERY_AND_MECHANICAL_INTEGRATION
```

The Survival Master must not become a cognition engine. Its job is to make work survive Android/Termux/tmux death and to preserve the single-SIGMA execution contract.

## GLOBAL CONTRACT

```text
ONE_SIGMA_AIL=YES
ONE_WRITER=YES
NO_STATE_FORK=MANDATORY
DEFAULT_SESSION=READ_PLUS_ARTIFACT_WRITE
HOST_COGNITION=NO
HOST_TEST_ORACLE=NO_FOR_SEMANTIC_RESULT
SIGMA_NATIVE_VERDICT=MANDATORY
CANONICAL_LEARN_COMMIT=LEASE_REQUIRED
BRAIN_WRITE_DEFAULT=REJECT
STATE_WRITE_DEFAULT=REJECT
MODEL_WRITE_DEFAULT=REJECT
LEARN_DEFAULT=REJECT
COMMIT_DEFAULT=REJECT
HEAD_CHANGE_DEFAULT=REJECT
MODEL_GENERATION_CHANGE_DEFAULT=REJECT
```

## LOCKED RUNTIME IDENTITIES — LAST VERIFIED PROJECT CONTRACT

```text
SIGMAC_SHA256=65f69217ad44f33c1aa1d4c31678d38940cd3d0b96f41892e8280dac57ad6a71
VM_SHA256=029ae4b6acbee5558f7663a732f8d39a970166e8488d2c4fe62414eb39391c99
VM_IS_GENESIS1=NOT_PROVEN
```

Identity mismatch means STOP. Do not silently rebuild, replace, or continue under the old capability claim.

## LIVE RUNTIME VALUES — LAST OBSERVED, MUST BE RECHECKED AFTER ANY ANDROID KILL

```text
LAST_OBSERVED_BRAIN_HEAD=700d5c1b4845322d7c14800029c629b0
LAST_OBSERVED_MODEL_GENERATION=1
SESSION_API=$HOME/SIGMA/sigma_genesis1/.sigma_ail/coordination/SESSION_R4/shell/sigma-session.bash
LAST_OBSERVED_G3_SESSION_CODE=SB5CBF5F836A7
LAST_OBSERVED_G3_SESSION_ACCESS=READ_PLUS_ARTIFACT_WRITE
LAST_OBSERVED_G3_SESSION_CANONICAL_MUTATION=REQUIRES_EXPLICIT_ADMISSION
CURRENT_PROCESS_LIVENESS=NOT_PROVEN_FROM_GITHUB
CURRENT_SESSION_LIVENESS=NOT_PROVEN_FROM_GITHUB
```

Never assume a historical SESSION_CODE is active after Termux was killed. In a surviving shell, call `sigma-session status`. In a new shell, source the API and let the coordinator grant the correct session. Never copy another pane's SESSION_CODE.

## CURRENT VERIFIED RESULT LEDGER

`DOCS/SIGMA_LATEST_VERIFIED_RESULT.md` currently records through `AIL-021`.

```text
LATEST_ENTRY=AIL-021
LATEST_PACKAGE=SIGMA_G3B_500_WEBTOOLS_R2
LATEST_RESULT=PASS_ARTIFACT_INTEGRITY_ONLY_RUNTIME_NOT_RUN
LATEST_FULL_PASS_ENTRY=AIL-019
LATEST_FULL_PASS_PACKAGE=SIGMA_G3B_200_STORY_DIRECT_INTERNET_NATIVE_CHALLENGE_R1_FIX3_POSTRUN_EVALUATION
ENTRY_COUNT=21
```

Do not reinterpret `LATEST_RESULT` as a production promotion.

## AIL-019 — 200 REAL INTERNET STORIES — EXACT VERIFIED SCOPE

The completed 200-story run was mechanically rehashed in a separate evaluation session.

```text
VALID_NATIVE_EVIDENCE_CHAIN_COUNT=200
DISTINCT_SOURCE_PAGE_ID_COUNT=200
DISTINCT_RAW_CONTENT_SHA256_COUNT=200
DISTINCT_SUMMARY_SHA256_COUNT=200
TOTAL_INPUT_WORD_COUNT_MECHANICAL=949039
TOTAL_SUMMARY_WORD_COUNT_MECHANICAL=199898
FINAL_MECHANICAL_VERIFIER_RECEIPT=PASS
SUMMARIZER_SOURCE_IDENTITY=PASS
SELECTOR_SOURCE_IDENTITY=PASS
PLANNER_SOURCE_IDENTITY=PASS
REAL_INTERNET_ACQUISITION=PASS_IN_EXACT_REHASHED_200_ITEM_SCOPE
NATIVE_SOURCE_SELECTION=PASS_IN_EXACT_REHASHED_200_ITEM_SCOPE
NATIVE_FULL_INPUT_CONSUMPTION=PASS_IN_EXACT_REHASHED_200_ITEM_SCOPE
NATIVE_OUTPUT_BYTES=PASS_IN_EXACT_REHASHED_200_ITEM_SCOPE
NATIVE_OUTPUT_MODE=NATIVE_EXTRACTIVE_THREE_SPAN_NARRATIVE_DIGEST
MECHANICAL_POSTRUN_AUDIT=PASS
```

The claim ceiling remains:

```text
ABSTRACTIVE_NARRATIVE_SUMMARIZATION=NOT_PROVEN
SEMANTIC_STORY_UNDERSTANDING=NOT_PROVEN
LEARNED_NARRATIVE_REPRESENTATION=NOT_PROVEN
PERSISTENT_LEARNING_FROM_THIS_RUN=NO_CANONICAL_MUTATION
RESTART_RECALL_GENERALIZATION=NOT_TESTED_BY_THIS_RUN
G3_LEARNED_NARRATIVE_BRAIN=NOT_PROVEN
G3_PROMOTION=NO
G6_PROMOTION=NO
SEMANTIC_GATE_VERDICT=NOT_PROVEN_REQUIRES_NATIVE_SIGMA_GATE
```

Uploaded FIX3 archive identity independently rechecked during documentation:

```text
FIX3_LIVE_SCREEN_R1_ARCHIVE_SHA256=77706e5ad8d81c01977d424f79ece109dd96a243bc0e569d560cdb7d074c5dd1
FIX3_LIVE_SCREEN_R1_MANIFEST_VERIFICATION=PASS
FIX3_RUNNER_SHA256=dcb6b906bfe53712f12c78e7462be9cfeb0a9e244ade4e92eaadae57c8495d92
FIX3_PLANNER_SOURCE_SHA256=7bd99c3cb3416aeb7c65cf819c38a5d48baeefb4626205515562f905d8acb32c
FIX3_SELECTOR_SOURCE_SHA256=ca6b919f22a9dcabeea69e8c58fe830cc2f031a2371b75e04d0407d672736bc6
FIX3_SUMMARIZER_SOURCE_SHA256=3d6af8258723a0da49739beb729b08e83af8e04d2a0da023d2fbe715e320ef2e3
FIX3_VERIFIER_SHA256=a0853efcfb2a8484e5e48df1ee3d67c5e5964d592ab4484542c1e29e9c48e94b
```

## AIL-020 — CURRENT G3 SELF-LEARNED SURFACE BLOCKER

Last observed run:

```text
SESSION_CODE=SB5CBF5F836A7
TASK=G3 test native cross-fit self-learned surface realization on completed 200-story Internet corpus; artifacts only
BUNDLE_SHA256=bd5eb9a42bf006187e7e08adb5b13a8600bac361e066d2d96fb8ffce203e29bd
LOCKED_TOOLCHAIN_IDENTITY_GATE=PASS
REP_SOURCE_SHA256=cdc93a24ce24a56fafd6489f6cf262d70b1b99109e198350edb35c16ed68b69f
REP_BYTECODE_SHA256=d5e34ad1afcdbeb33d0bd9809cfa08731e14bef5f03f302aafced498a0c35789
REP_COMPILE=PASS
LEARN_COMPILE=FAIL
LEARN_COMPILE_ERROR=sigmac_line_46_col_2_top-level_item_must_be_DEF_or_⟡_command_token_⚡
NATIVE_RUNTIME_PROOF=NOT_RUN_YET
G3_PROMOTION=NO
```

Correct next action for this lane:

```text
FIX_NATIVE_LEARN_SOURCE_SYNTAX_MINIMALLY
-> preserve original failure evidence
-> rerun the same locked compile gate
-> if compile passes, freeze new source + bytecode hashes
-> only then continue the unchanged native runtime admission suite
```

Do not change the semantic gate merely to obtain PASS. Do not rerun unchanged source expecting a different compile result.

## AIL-021 — 500-WEBTOOLS R2 — WHAT IT IS AND IS NOT

Uploaded archive:

```text
WEBTOOLS_R2_ARCHIVE_SHA256=e1116f2eb0c43976058f6b5d1af5c678f23a1be7fbb2ab66f966487a245332d0
WEBTOOLS_R2_MANIFEST_VERIFICATION=PASS
SOURCE_FIX3_ARCHIVE_SHA256=77706e5ad8d81c01977d424f79ece109dd96a243bc0e569d560cdb7d074c5dd1
NATIVE_TOOL_IO_V1_SOURCE_SHA256=cf61c4e4e00f31dd51d6781aa50dd66316b8ebc52bc70e921b0d6fcebe14dfb4
RUN_NATIVE_WEBTOOLS_R2_SHA256=5b43d82b989cdcc9e9179bda7e5ea0c3d5a0e6dbf53788013a868f354678db0f
TOOL_BRIDGE_SHA256=03e189e058244dbf3feffe67c9c947e581a7f6daae2ce1b97c09e8020a9de47a
PUBLIC_SUFFIX_LIST_SHA256=a26f7d7e334778ed69216cedb5451ef82031feba6615c12039783cdd94e1fcae
MECHANICAL_TESTS_SHA256=2e5602e145cc928b3547c7b628c251d4adfad6574f6e3e7fbc8533d57c254f22
```

Declared challenge target:

```text
WEBSITE_COUNT_MIN=5000
STORY_COUNT_MIN=500
INPUT_WORDS_PER_STORY_MIN=200000
SUMMARY_WORDS_PER_STORY_MIN=1000
HOST_WRITTEN_QUERY=NO
HOST_SOURCE_SELECTION=NO
HOST_SUMMARY_WRITING=NO
HOST_SEMANTIC_ANSWER=NO
HOST_COGNITION=NO
EXTRACTIVE_THREE_SPAN_ACCEPTABLE_FOR_FINAL_500_STORY_TARGET=NO
```

Current proof:

```text
SIGMA_G3B_500_WEBTOOLS_R2_ARTIFACT_INTEGRITY=PASS
LOCKED_SIGMAC_COMPILE=NOT_RUN_HERE
LOCKED_VM_RUNTIME=NOT_RUN_HERE
LIVE_INTERNET_TRANSPORT_TEST=NOT_RUN_HERE
TMUX_DEVICE_TEST=NOT_RUN_HERE
AUTONOMOUS_NATIVE_CONTROLLER=NOT_IMPLEMENTED_BY_THIS_TOOL_PACKAGE
LEARNED_NATIVE_SUMMARY_GENERATOR=NOT_PRESENT_IN_SUPPLIED_G3B_BUNDLE
FULL_TASK_COMPLETE=NO
```

The tool package is a mechanical substrate. It MUST NOT be treated as the missing cognition/controller.

## EXACT WEBTOOLS OPERATIONS ALREADY AVAILABLE

The R2 mechanical bridge exposes these operations, driven by a native controller:

```text
HTTP_GET
DECODE_TEXT
READ_RANGE
READ_TEXT_RANGE
ACK_RANGE
PUBLISH_SUMMARY
STATUS
FINISH
```

Ownership rule:

```text
NATIVE_SIGMA=chooses research intent/query/source/resource/range grouping/summary/next action
HOST=network transport/protocol decode/exact byte movement/hash/accounting/persistence only
```

A website is counted mechanically by registrable domain under the packaged Public Suffix List, including PRIVATE rules. A URL or subdomain is not automatically a distinct website.

## INTERNET CAPABILITIES ALREADY PROVEN — DO NOT REBUILD FROM ZERO

### On branch `AIL_SIGMA`

The sequential AIL ledger already records these bounded capabilities:

```text
AIL-004: SIGMA_AUTONOMOUS_WEB_DISCOVERY=PASS_IN_BOUNDED_SEED_CRAWL_SCOPE
AIL-004: SIGMA_AUTONOMOUS_WEB_LINK_SELECTION=PASS_IN_BOUNDED_SEED_SCOPE
AIL-006: SIGMA_AUTONOMOUS_WEB_LEARNING=PASS_IN_BOUNDED_THREE_DOCUMENT_SCOPE
AIL-006: SIGMA_MEMORY_REPLAY=PASS_IN_EXACT_TESTED_SCOPE
AIL-006: SIGMA_OFFLINE_LEARNING=PASS_IN_REPLAY_WITHOUT_RAW_OR_NETWORK_REQUEST_SCOPE
AIL-006: MODEL_STATE_PERSISTS_ACROSS_FRESH_PROCESS=PASS
AIL-007: SIGMA_DURABLE_MEMORY_LINK_ACCESS=PASS_IN_EXACT_24_LINK_SNAPSHOT_SCOPE
AIL-007: SIGMA_SELF_SELECTS_RECALLED_LINKS=PASS_IN_24_TURN_VISITED_SLOT_SCOPE
AIL-008: SIGMA_SELF_SELECTS_WHAT_TO_STUDY_NEXT=PASS_IN_FOUR_FETCHABLE_RECALLED_LINK_PROBE_SCOPE
AIL-008: SIGMA_RECALLED_LINK_TO_WEIGHT_LEARNING=PASS_IN_EXACT_TESTED_SCOPE
AIL-012: SIGMA_MULTI_BOOK_LEARNING=PASS_IN_THREE_DISTINCT_16KB_BOOK_CHUNK_SCOPE
AIL-012: OFFLINE_MULTIBOOK_REPLAY=PASS
AIL-013: SIGMA_FULL_DOCUMENT_BYTE_TRAVERSAL=PASS_IN_ONE_BOUNDED_GUTENBERG_TEXT_SCOPE
AIL-013: SIGMA_LONG_DOCUMENT_SEQUENTIAL_LEARNING=PASS_IN_ONE_FULL_DOCUMENT_SCOPE
AIL-015: SIGMA_CONTINUOUS_SHADOW_CONTROL=PASS_IN_INSTALLED_TWO_HOUR_CYCLE_POLICY
AIL-016: R8_OUTER_RESURRECTION=PASS_IN_DEAD_SUPERVISOR_RESTART_SCOPE
AIL-019: REAL_INTERNET_ACQUISITION=PASS_IN_EXACT_REHASHED_200_ITEM_SCOPE
AIL-019: NATIVE_SOURCE_SELECTION=PASS_IN_EXACT_REHASHED_200_ITEM_SCOPE
AIL-019: NATIVE_FULL_INPUT_CONSUMPTION=PASS_IN_EXACT_REHASHED_200_ITEM_SCOPE
```

Preserve each exact scope. Do not widen a bounded result into general semantic understanding.

### Cross-branch proven Internet research capabilities on `SIGMA_LIFE`

These are provenance references to exact machine-evidence commits. They are not automatically code-merged into `AIL_SIGMA`, but they are important because future work must reuse the capability design rather than invent host cognition:

```text
V10 autonomous replan -> fresh Internet loop
commit=842e643be790e4e4e4f2b96554d768bdc6824a6d
SIGMA_NATIVE_REPLAN_TO_FRESH_INTERNET_LOOP=PASS_TESTED_SCOPE

V11 strategy-conditioned query adaptation
commit=dd34f2863ccf11d9ac48470eff80bb9dc42cc957
SIGMA_NATIVE_STRATEGY_CONDITIONED_QUERY_ADAPTATION=PASS_TESTED_SCOPE
HOST_QUERY_COMPOSITION=NO

V12 query outcome feedback + diversity
commit=4651fa05101fe2876e8b64ecae20ddbc3a51450e
SIGMA_NATIVE_QUERY_OUTCOME_FEEDBACK_AND_DIVERSITY=PASS_TESTED_SCOPE
OUTCOME_DECISION_PLANE=SIGMA_NATIVE_VM
QUERY_DIVERSITY_PLANE=SIGMA_NATIVE_VM
HOST_QUERY_SCORING=NO
HOST_QUERY_COMPOSITION=NO

I4 native source-family selection
commit=44d22362ce588b9be741ca7cd35e74a955eea354
NATIVE_SOURCE_FAMILY_SELECTION=PASS_IN_EXACT_TESTED_SCOPE
HOST_SOURCE_SELECTION=NO
HOST_CATALOG_RANKING=NO
HOST_RESOURCE_SELECTION=NO

I5B native Wikipedia candidate/resource selection
commit=86358984c29edb63988d413e3d85441c3409cf9b
NATIVE_RESOURCE_SELECTION=PASS_IN_EXACT_TESTED_SCOPE
HOST_CANDIDATE_SELECTION=NO
HOST_RESOURCE_SELECTION=NO

I5C native-selected resource -> exact Internet fetch binding
commit=c26a858a16fa8b5f8ab011908591661b24bd44d8
SIGMA_SELECTED_RESOURCE_TO_V5K2_EXACT_FETCH=PASS_IN_EXACT_TESTED_SCOPE
HOST_HTTP_ROLE=MECHANICAL_EXACT_REQUEST_TRANSPORT_ONLY
HOST_JSON_DECODE_ROLE=MECHANICAL_EXACT_PROTOCOL_DECODE_ONLY
```

Do not claim these cross-branch artifacts are currently active in the AIL runtime unless exact bytes/hashes/runtime bindings are verified on Oppo.

## SOURCE UNIVERSE — IMPORTANT COUNTING BOUNDARY

Historical large source-universe evidence on `SIGMA_LIFE` recorded:

```text
DISCOVERY_ROWS=41644
DISCOVERY_TARGET_ROWS=41588
DISCOVERY_SEED_METADATA_ROWS=56
DIRECT_URL_RAW_OCCURRENCES=1198
DIRECT_URL_UNIQUE_CANONICAL=1057
EXACT_CANONICAL_URL_DUPLICATE_GROUPS=130
DUPLICATE_EXCESS_OCCURRENCES=141
```

Important:

```text
41644 rows != 41644 verified websites
```

The discovery universe is large, but do not convert metadata rows into a false verified-website count.

## SURVIVAL — WHAT IS HISTORICALLY PROVEN

The AIL ledger proves:

```text
AIL-015 R8_CHILD_CRASH_RECOVERY=PASS
AIL-015 R8_CYCLE_DEDUPLICATION=PASS
AIL-015 R8_SHADOW_BOOT_RESURRECTION_INSTALLED=PASS
AIL-016 R8_DEAD_SUPERVISOR_RECOVERY=PASS
AIL-016 R8_OUTER_RESURRECTION=PASS_IN_DEAD_SUPERVISOR_RESTART_SCOPE
```

But AIL-016 explicitly keeps:

```text
ANDROID_WHOLE_TERMUX_PROCESS_GROUP_SURVIVAL=NOT_PROVEN
R8_NATURAL_POST_RESTART_CYCLE=NOT_YET_PROVEN
```

This distinction is central. `tmux` protects against terminal detachment; it does not protect against Android killing the entire Termux process group.

Last operator-controlled state known to this handoff: do not assume the historical survival daemons are currently alive. Inspect Oppo first. Do not restart old survival automation blindly because a stale supervisor can create duplicate workers or writer conflicts.

## SURVIVAL NORTH STAR

The target is not merely:

```text
PROCESS_NEVER_DIES
```

The correct target is:

```text
TASK_NEVER_LOSES_COMMITTED_PROGRESS
EXACTLY_ONE_ACTIVE_WORKER_PER_RUN
CHECKPOINT_REHASH_BEFORE_RESUME
RESUME_SAME_TASK_NOT_NEW_SEMANTIC_DECISION
NO_SECOND_CANONICAL_WRITER
NO_STATE_FORK
RECOVERY_IS_MECHANICAL_ONLY
```

Android may kill SIGMA. After Termux returns, the mechanical survival layer must restore the exact unfinished run from the last valid committed checkpoint, not invent a new lesson, website, query, result, or learning decision.

## ONE SIGMA, TWO ACTIVE LANES — TARGET ARCHITECTURE

The intended architecture is one SIGMA with two concurrent work lanes, not two independent SIGMAs:

```text
ONE SIGMA.AIL
│
├── LOCAL LEARNING LANE
│   ├── consumes a sealed learning snapshot
│   ├── updates candidate weights/model under its admitted learning authority
│   ├── checkpoints frequently
│   └── canonical mutation only under separate explicit admission/lease
│
└── INTERNET AUTOLEARN LANE
    ├── SIGMA detects gap/research intent
    ├── SIGMA forms/adapts query
    ├── SIGMA selects source family
    ├── SIGMA selects exact resource
    ├── host transports exact public bytes only
    ├── SIGMA reads/evaluates/compares evidence
    ├── SIGMA creates its own compact representation / compression
    ├── SIGMA decides KEEP / RESEARCH_MORE / TRY_ALTERNATIVE / UNKNOWN / admission request
    └── writes immutable provenance-bound artifacts / queue entries
```

The Internet lane may run while the local trainer is training. It MUST NOT concurrently mutate the same canonical weight/model state.

## ONLINE DATA VS WEIGHT LEARNING

These are separate operations:

```text
ACQUIRE / READ / EVALUATE / COMPRESS INTERNET EVIDENCE
!=
UPDATE CANONICAL MODEL WEIGHTS
```

Best current integration pattern:

```text
Internet evidence
-> exact hash + provenance
-> SIGMA-native assessment
-> SIGMA-native compact representation / memory candidate
-> immutable sealed learning packet
-> native SIGMA learning/admission decision
-> ONE admitted weight writer
-> checkpoint
-> evaluation
-> explicit promotion only if separately admitted
```

Never wire:

```text
web page -> curl/python filter -> trainer -> canonical weights
```

because that moves source/semantic selection into the host and destroys replayable provenance.

## SIGMA-NATIVE COMPRESSION REQUIREMENT

The user goal includes: SIGMA reads Internet material and compresses it into SIGMA's own useful representation.

The rule is:

```text
SIGMA decides what information is retained
SIGMA emits the compact representation bytes/state
HOST stores the exact emitted bytes mechanically
HOST does not invent semantic fields, summaries, lessons, labels, truth states, or compression policy
```

Do not invent a host-side "Sigma language" schema and then claim SIGMA compressed the material. If a compact internal representation format is required, the semantic construction must execute in native `.sigma`; host may only serialize exact native output into storage primitives.

A compact representation is not automatically semantic understanding. Keep claim scope tied to runtime tests.

## WEBSITE PROGRAM — REQUIRED COGNITIVE OWNERSHIP

Any future autonomous website program must preserve this ownership chain:

```text
SIGMA native state
-> native gap/goal
-> native query or discovery request
-> native source-family decision
-> native candidate/resource selection
-> host exact network transport
-> native full-input reading in bounded resumable ranges
-> native evidence/quality/comparability decision
-> native compact representation / summary / knowledge candidate
-> native next action
-> optional native learning proposal
```

Forbidden host substitutions:

```text
HOST_QUERY_GENERATION=NO
HOST_SOURCE_SELECTION=NO
HOST_RESOURCE_SELECTION=NO
HOST_RESULT_RANKING=NO
HOST_SEMANTIC_FILTERING=NO
HOST_SUMMARY_WRITING=NO
HOST_LESSON_SELECTION=NO
HOST_TRUTH_DECISION=NO
HOST_NEXT_ACTION_SELECTION=NO
HOST_WEIGHT_UPDATE=NO_UNLESS_MECHANICAL_COMMIT_OF_NATIVE_ADMITTED_CANDIDATE
```

The host may refuse unsafe/malformed/oversized network operations mechanically. A refusal returns an observation to SIGMA; the host must not silently choose a substitute site.

## CURRENT 500-WEBTOOLS INTERFACE — REUSE, DO NOT BYPASS

The provided native IO definitions already support a one-request-per-VM-invocation state machine. A controller should persist native state and on each invocation either emit exactly one operation or finish.

Conceptual native loop:

```text
LOAD_NATIVE_STATE
-> inspect prior mechanical response
-> native decision
-> emit one WT_* request
-> persist native state
-> exit VM

host executes exact request mechanically
-> writes exact response/provenance files

next VM invocation
-> native reads response
-> continues native decision process
```

This design is restart-friendly because cognition stays in persisted native state and host actions are replayable mechanical events.

## FULL-INPUT RULE

For a source to count as consumed:

```text
exact source object identity
+ content hash
+ bounded range delivery
+ no missing byte gaps
+ native ACK for delivered ranges
+ completion coverage receipt
```

Do not equate download success with reading. Do not truncate a response and mark it complete. If a size limit is hit, return the limit condition to native SIGMA.

## FINAL SUMMARY RULE FOR FUTURE 500-STORY TARGET

The new 500-story target explicitly rejects the old extractive three-span method as the final answer:

```text
EXTRACTIVE_THREE_SPAN_ACCEPTABLE_FOR_FINAL_500_STORY_TARGET=NO
```

A new summary/representation capability must be native, input-conditioned, dynamically tested, anti-hardcode audited, and independently verified after VM output. Host cannot write or repair summary text.

## SINGLE-WRITER INTEGRATION WITH LOCAL TRAINER

The correct concurrent model is:

```text
INTERNET LANE = READ + ARTIFACT_WRITE + immutable evidence/memory packets
LOCAL TRAINER = candidate model writer in its admitted workspace
CANONICAL WRITER = only one lease holder at a time
```

If Internet SIGMA wants new evidence learned into weights while a long local run is active, it should queue a provenance-bound candidate batch rather than modify the trainer's current sealed dataset mid-run.

After the local run completes:

```text
validate final checkpoint
-> preserve generation identity
-> native SIGMA reviews queued learning material
-> seal next dataset/snapshot
-> start next admitted training generation
```

This keeps the current run reproducible and allows Internet acquisition to continue continuously.

## RECOVERY MODEL — FUTURE IMPLEMENTATION REQUIREMENT

A survival implementation must persist at least the following mechanical recovery facts outside volatile tmux state:

```text
WORK_ID
TASK_KIND
EXACT_RUN_COMMAND_OR_ENTRYPOINT
ENTRYPOINT_SHA256
DEPENDENCY_HASHES
SOURCE_RUN_ID
CURRENT_CHECKPOINT_PATH
CURRENT_CHECKPOINT_SHA256_OR_MANIFEST_ROOT
LAST_COMMITTED_PROGRESS_MARKER
CANONICAL_HEAD_AT_OPEN
MODEL_GENERATION_AT_OPEN
WRITER_REQUIRED=YES|NO
WRITER_LOCK_ID_IF_ANY
ARTIFACT_ROOT_OR_DURABLE_RUN_ROOT
LAST_HEARTBEAT
LAST_COMPLETION_RECEIPT
```

The survival layer may choose only between mechanical states such as:

```text
NO_PROCESS_AND_VALID_UNFINISHED_CHECKPOINT -> restart exact worker
PROCESS_ALREADY_ALIVE -> do nothing
COMPLETION_RECEIPT_PRESENT -> do not restart
IDENTITY_MISMATCH -> HOLD
CHECKPOINT_INVALID -> HOLD
WRITER_LOCK_CONFLICT -> HOLD
```

It must not choose what SIGMA should study next.

## RECOVERY CLAIMS MUST BE LAYERED

Do not claim full Android survival after only a child restart. Prove separately:

```text
L1_CHILD_PROCESS_CRASH_RECOVERY
L2_WORKER_WRAPPER_CRASH_RECOVERY
L3_SUPERVISOR_CRASH_RECOVERY
L4_TMUX_SERVER_DEATH_RECOVERY
L5_TERMUX_PROCESS_GROUP_DEATH_AND_APP_RESTART_RECOVERY
L6_DEVICE_REBOOT_RECOVERY
L7_CHECKPOINT_RESUME_NO_DUPLICATE_COMMIT
L8_SINGLE_WRITER_NO_FORK_UNDER_RECOVERY_RACE
```

Each level requires exact machine evidence and a bounded claim.

## WHAT A NEW WINDOW MUST DO — FIRST 5 MINUTES

1. Read the four Survival Master files named at top.
2. Do not ask the user to restate project history already here.
3. On Oppo, inspect exact session/runtime liveness using the commands in `SURVIVAL_MASTER/SAMPLES/COMMANDS_R1.md`.
4. Compare live BRAIN_HEAD/model generation/tool hashes with the last observed values. Do not mutate them manually.
5. Determine which exact unfinished work item is active:
   - local trainer;
   - G3 self-learned-surface compile repair;
   - Internet native controller;
   - survival/recovery implementation;
   - evaluation only.
6. Continue only one concrete next step. Do not broad-scan or rebuild admitted components.

## CURRENT PRIORITY ORDER

Unless the human explicitly changes priority:

```text
P0 = preserve / resume long-running local learning after Android death without fork
P1 = build and admit survival journal + exact checkpoint resume path
P2 = build native Internet controller on top of WEBTOOLS_R2, reusing proven SIGMA-native Internet selection/query patterns
P3 = prove native compact representation / non-three-span summarization over full Internet inputs
P4 = connect Internet evidence queue to the next local learning generation through separate native admission
P5 = only after proof, consider canonical production cutover/promotion
```

The currently failed G3 self-learned-surface compile should be repaired minimally when that lane is active; it is not authority to block Survival design documentation.

## DO NOT DO THESE THINGS

```text
DO_NOT copy another pane's SESSION_CODE
DO_NOT manually edit BRAIN_HEAD
DO_NOT manually edit MODEL_GENERATION
DO_NOT kill all tmux sessions as a recovery strategy
DO_NOT restart a worker without checking for an existing matching worker
DO_NOT create a second canonical writer
DO_NOT let Python/Bash choose websites, queries, summaries, semantic labels, learning batches, or winners
DO_NOT treat download as understanding
DO_NOT treat artifact integrity as runtime admission
DO_NOT treat compile success as semantic proof
DO_NOT treat 200-story extractive output as abstractive understanding
DO_NOT treat 41644 discovery rows as 41644 verified websites
DO_NOT silently merge `SIGMA_LIFE` capability code into `AIL_SIGMA` without exact hash/dependency verification
DO_NOT promote merely because a candidate passed a bounded test
```

## CHECKPOINTING POLICY FOR FUTURE WINDOWS

After every meaningful milestone, update this file or append a small dated section to it containing:

```text
DATE
BRANCH_HEAD_BEFORE
RUNTIME_SESSION_CODE
WORK_ID
OPEN_HEAD
OPEN_MODEL_GENERATION
EXACT_SOURCE_HASHES
EXACT_BYTECODE_HASHES
RUN/ARTIFACT_ROOT
MACHINE_RESULT
CLAIM_SCOPE
HOLD/FAIL_REASON_IF_ANY
CURRENT_NEXT_ACTION
BRANCH_COMMIT_CREATED
```

Do not paste huge terminal logs into this master file. Put raw evidence in an exact artifact or immutable capability checkpoint; keep this file as the continuity index.

## HUMAN-FACING MENTAL MODEL

The intended system is simple from the operator's point of view:

```text
ONE SIGMA
+ local learner
+ Internet research/reading lane
+ durable checkpoints
+ survival recovery
+ one canonical writer
```

The human should not have to manually shuttle structured KEY=VALUE messages between tmux windows. Sessions, work IDs, receipts, checkpoints and provenance should be mechanically bound by the system.

## NEXT WINDOW STOP RULE

If context is uncertain, do not rediscover the entire project. Stop and read:

```text
SURVIVAL_MASTER/CURRENT_HANDOFF.md
SURVIVAL_MASTER/RUNBOOK/SURVIVAL_INTERNET_AUTOLEARN_RUNBOOK_R1.md
SURVIVAL_MASTER/SAMPLES/COMMANDS_R1.md
DOCS/SIGMA_LATEST_VERIFIED_RESULT.md
```

Then inspect only the exact current runtime state needed for the next action.
