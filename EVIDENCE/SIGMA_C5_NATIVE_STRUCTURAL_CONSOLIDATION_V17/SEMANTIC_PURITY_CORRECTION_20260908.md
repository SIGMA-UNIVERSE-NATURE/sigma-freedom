# Semantic-purity correction — 2026-09-08

A subsequent source audit found that the V13–V17 native core lineage contains a legacy discourse path with explicit lexical semantic mappings (for example, cue words mapped directly to relation labels) and LEFT/RIGHT clause splitting.

Therefore:

- V17 remains evidence of **structural** consolidation/rehydration of the admitted native state.
- V16 remains evidence of **structural** multi-provenance identity hypothesis behavior.
- V13 remains evidence of delayed native persistence and one-commit memory lifecycle.
- None of these versions should be cited as evidence that SIGMA learned discourse semantics from evidence or fully eliminated LEFT/RIGHT cognition.
- V18 Stage-6B semantic admission is rejected and must be redesigned from a clean semantic-purity baseline.

The next candidate must remove lexical word→semantic-relation tables and LEFT/RIGHT discourse cognition before semantic-learning claims resume.
