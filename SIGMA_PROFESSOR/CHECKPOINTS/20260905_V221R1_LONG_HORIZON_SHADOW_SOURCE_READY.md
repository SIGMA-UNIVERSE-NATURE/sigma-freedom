# V2.21R.1 — LONG-HORIZON SHADOW STABILITY/RECOVERY — SOURCE READY

Date: 2026-09-05 (Asia/Ho_Chi_Minh)

## Candidate

Runner:
`SIGMA_PROFESSOR/artifacts/RUN_SIGMA_V221R1_LONG_HORIZON_SHADOW_STABILITY_RECOVERY_PREFLIGHT.sh`

SHA256:
`c6ab9129af4692c4e134c39b088917c047864ccbf977a5314c0dc0f9322b0f3d`

README:
`SIGMA_PROFESSOR/artifacts/SIGMA_V221R1_LONG_HORIZON_SHADOW_STABILITY_RECOVERY_PREFLIGHT_README.txt`

Static shell parse:
`BASH_N_RC=0`

## Admission target

Prove long-horizon stability of the admitted native curriculum/fairness chain in isolated shadow state across six fairness scheduling boundaries and at least four real works.

Required properties:

- locked sigmac and VM v09 identities visible and equality-gated;
- production V2.4 remains running with the same runner PID;
- real first revisit defer/resume/redefer sequence survives;
- third real work completes its previously admitted real branch;
- first generation `||||` branch is not oracle-hardcoded;
- fourth real work completes initial deep/revalidation/lifecycle without branch oracle;
- persisted scheduled-event intent is recovered by a fresh host process at every extended boundary;
- at least four selector dispatch records;
- at least two fairness `PENDING` and two `RESUMED` records;
- shadow BRAIN remains isolated from production BRAIN.

Allowed claim after locked-runtime PASS only:

`LONG_HORIZON_SHADOW_STABILITY=PROVEN_IN_SIX_BOUNDARY_FOUR_REAL_WORK_SCOPE`

## Promotion status

Even after a V2.21 PASS:

`PRODUCTION_PROMOTION_ALLOWED=NO`

because:

`MID_APPEND_CRASH_ATOMICITY=NOT_PROVEN`

and current ABI append/write operations are not proven crash-atomic.

Next capability after PASS: crash-consistent transactional state protocol/recovery.
