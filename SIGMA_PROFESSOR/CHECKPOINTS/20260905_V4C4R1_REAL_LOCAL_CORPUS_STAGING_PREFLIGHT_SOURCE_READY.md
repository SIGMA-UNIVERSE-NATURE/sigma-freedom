# V4-C4 R1 — Real Repository Local Corpus Staging Preflight — SOURCE READY

Date: 2026-09-05 (Asia/Ho_Chi_Minh)
Branch: `SIGMA_LIFE`
Status: `SOURCE_READY / RUNTIME_NOT_RUN / PRODUCTION_BINDING_NO`

## Dependency now satisfied

The preceding native curriculum-stage fixture admission is recorded at:

```text
SIGMA_PROFESSOR/CHECKPOINTS/20260905_V4C4R1_LOCAL_FIRST_CURRICULUM_STAGE_PREFLIGHT_PASS.md
PASS_CHECKPOINT_CREATE_COMMIT=621de17aaefbcb9ec85f40ba4f924ab176f4053f
```

Observed V4-C4 R1 identity from that locked-Termux PASS:

```text
C4_SOURCE_GIT_BLOB=9c55b842b321feba5d755ef7021ba5a3067ff6e1
C4_SOURCE_SHA256=82946a22df4f409ba9c3fab6a24f47ba4ee496693bfd1085a23c1905b26cf9f1
C4_BYTECODE_SHA256=32dafa63db03bea0c6213d3c2fadb91e56c879b2d572836d2d82c871a9c18a76
```

## New exact runner

```text
RUNNER_PATH=SIGMA_PROFESSOR/artifacts/RUN_SIGMA_V4C4R1_REAL_LOCAL_CORPUS_STAGING_PREFLIGHT.sh
RUNNER_CREATE_COMMIT=c9412382df575efb8ec9fa30872d1800c55a50f5
RUNNER_GIT_BLOB=0461f4cf69dd24de50dd6b9ca018c70f109cb37d
RUNNER_SHA256=NOT_YET_CANONICALLY_OBSERVED_ON_TERMUX
```

## Purpose

The next missing evidence is not another synthetic LOCAL/EXTERNAL decision case. It is whether the actual repository-local stored/taught corpus can be mechanically exposed to the already-passed native V4-C4 R1 controller without turning Bash into a teacher.

This preflight therefore stages exact tracked text bytes from the fixed repository storage/teaching roots:

```text
54_CORES
BRAIN
BẢN ĐỒ
CORE
DATA
DOCS
EXPERIENCE
CHAT_WITH_SIGMA.txt
FREEDOM_PROOF.md
```

The root scope is broad and mechanical; the host does not rank topics, select lessons, summarize, rewrite, score, or decide relevance.

## Mechanical exclusions

The runner excludes only explicit control/security/result classes:

```text
BRAIN/KEYS/**
*/KEYS/**
RESULTS/**
*/RESULTS/**
REPORTS/**
*/REPORTS/**
.env / .env.*
*.pem *.key *.p12 *.pfx *.keystore *.jks
symlinks
empty files
non-text/binary files as mechanically detected by grep -I
SIGMA_PROFESSOR/**  # never enters the configured source roots
```

This keeps `DO_NOT_LOAD_RESULTS=YES` and avoids credential/key ingestion. It is not semantic curriculum filtering.

`54_CORES/*.py`, when mechanically detected as text, is copied only as historical/reference bytes. It is never executed as SIGMA cognition:

```text
ACTIVE_PYTHON_COGNITION=NO
PYTHON_EXECUTED_AS_COGNITION=NO
```

## Content-addressed staging

For every eligible tracked source file, the runner computes mechanically:

```text
SOURCE_CONTENT_SHA256
SOURCE_PATH_SHA256
SOURCE_GIT_BLOB
SOURCE_SIZE_BYTES
DOCUMENT_ID=SHA256(source_path || NUL || source_content_sha256)
```

It copies the exact source bytes to:

```text
<isolated-local-raw>/<DOCUMENT_ID>.document
```

and writes a separate provenance record containing the content hash, path hash, base64-encoded exact source path, Git blob, size, and exact-copy assertion.

No semantic transformation is performed.

## Runtime question this preflight is designed to answer

The actual repository corpus may be much larger than the seven fixture cases. V4-C4 R1 currently counts local suffixes natively. Therefore the real-corpus run is also an explicit scalability/step-limit probe.

A PASS may establish only:

```text
REAL_REPOSITORY_TRACKED_LOCAL_TEXT_STAGING=PASS_IN_OBSERVED_ROOT_AND_EXCLUSION_SCOPE
EXACT_SOURCE_BYTE_COPY_AND_SHA_BINDING=PASS
PER_DOCUMENT_PATH_CONTENT_GIT_BLOB_PROVENANCE=PASS
C4_REAL_CORPUS_SCALE_VM_EXECUTION=PASS_IN_OBSERVED_STAGED_COUNT_SCOPE
NATIVE_LOCAL_SELECTION_ON_REAL_STAGED_CORPUS=PASS_IN_PREFLIGHT_SCOPE
```

If the C4 VM hits a step limit or other failure at the real staged count, preserve that failure. Do not reduce the corpus or weaken the gate merely to obtain PASS; repair the smallest bounded native/census layer instead.

## Still not executed/proven

```text
REAL_LOCAL_CORPUS_LEARNING_EXECUTION=NOT_EXECUTED
REAL_LOCAL_CORPUS_FULL_OPERATIONAL_PASS=NOT_EXECUTED
REAL_LOCAL_TO_EXTERNAL_CONTINUOUS_TRANSITION=NOT_EXECUTED
V4_EXTERNAL_CONSUMPTION_OF_RUNNING_V2_4_NATIVE_FETCH_FEED=NOT_EXECUTED
LIVE_V4_HTTP_FETCH=NOT_EXECUTED
LOCAL_SEMANTIC_MASTERY=NOT_CLAIMED
PRODUCTION_BINDING=NO
V4_PRODUCTION_PROMOTION_ALLOWED=NO
PRODUCTION_V2_4_KEEP_RUNNING=YES
```

The semantic claim-ledger boundary is external engineering bookkeeping only and must not be injected as SIGMA's self-description.

## Next action

Run this exact preflight on the locked Termux runtime. Return either the first `HOLD=`/failure or the final summary beginning with:

```text
V4C4R1_REAL_LOCAL_CORPUS_STAGING_PREFLIGHT=
```

Only after real-corpus staging and C4 scale execution PASS should the infinite V4-C4 local-first continuous shadow runner be bound to this corpus snapshot and the existing native external feed.
