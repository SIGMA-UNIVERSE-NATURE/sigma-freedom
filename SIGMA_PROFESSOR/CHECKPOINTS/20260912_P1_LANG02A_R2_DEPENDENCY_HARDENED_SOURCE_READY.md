# P1 / LANG-02A R2 — DEPENDENCY-HARDENED LOCKED-VM PREFLIGHT SOURCE READY

Date: 2026-09-12 (Asia/Ho_Chi_Minh)
Branch: `candidate/p1-lang02a-r2-20260912`
Status: `SOURCE_READY / LOCKED_RUNTIME_NOT_RUN`

## Purpose

P1 is the smallest currently valid language capability frontier toward the long-horizon SIGMA autonomy goal:

`LANG-02A_NATIVE_OPERATOR_SCOPE_BINDING`

This checkpoint does **not** claim natural-language negation, semantic scope, proposition truth, human-language understanding, or general autonomous learning.

The native cognition source is intentionally unchanged from R1. R2 is a runner-only evidence hardening step after static audit found that R1 wrote the LANG-01A dependency capability/status strings as fixtures without first equality-gating the canonical admitted dependency record.

## Native cognition — unchanged

Native source:

`SIGMA_PROFESSOR/artifacts/SIGMA_LANG_02A_NATIVE_OPERATOR_SCOPE_BINDING_V1.sigma`

Identity:

`SOURCE_SHA256=7a40e92e11c7c89574d3b975bb3210a7a7a23690251951da68be9e7edbfe292b`

`SOURCE_GIT_BLOB=226b06c337c09f3e0dc3f35a44c3ba22d73affaf`

`SOURCE_ORIGINAL_COMMIT=32df3e826656f6c06ec6379b8bc3a378ce446bf4`

`NATIVE_SOURCE_CHANGED_FROM_R1=NO`

The capability remains structural only:

- exactly one explicitly supplied operator record;
- bounded structural scope records;
- inclusive enclosure;
- unique minimum enclosing width selects a structural scope;
- equal minimum width across distinct scope IDs remains ambiguous;
- no enclosing scope fabricates no binding;
- exact duplicate scope identity is idempotent;
- same scope ID with a different fingerprint is refused;
- capacity remains 8 unique scopes;
- encounter order is not winner policy.

## Locked runtime

`SIGMAC_SHA256=65f69217ad44f33c1aa1d4c31678d38940cd3d0b96f41892e8280dac57ad6a71`

`VM_SHA256=029ae4b6acbee5558f7663a732f8d39a970166e8488d2c4fe62414eb39391c99`

Both identities are equality-gated before the R2 suite is allowed to proceed.

## Canonical LANG-01A dependency lock

Canonical admission record:

`SIGMA_PROFESSOR/CHECKPOINTS/TEACHER_GPT_LANGUAGE_LANE_CURRENT.md`

Pinned Git blob:

`DEPENDENCY_CHECKPOINT_GIT_BLOB=d2d6a29c55561170dc19b6aec3c666e7e26011c6`

R2 requires exact lines proving:

`LANG-01A — Native Distributional Event-Frame Hypothesis Induction — ADMITTED`

`DEPENDENCY_SOURCE_SHA256=a47142af96dcbc47f0181f38952d29a5fa6fffeeb6c24b466b6b3a5acc759310`

`DEPENDENCY_BYTECODE_SHA256=bb584206f8db16d832e2c20a027c4673e34ab9484ad062cd0f7bbe01206eafc9`

`DEPENDENCY_TOTAL_VM_INVOCATIONS=10`

and the recorded persistence/restart/negative/bounded-input-refusal PASS.

Only after that immutable admission record lock passes may the runner mechanically write the existing dependency ABI fixture strings consumed by native LANG-02A.

Therefore:

`DEPENDENCY_FIXTURE_STRING_ALONE_IS_PROOF=NO`

`DEPENDENCY_EXACT_ADMISSION_RECORD_REQUIRED_BEFORE_FIXTURE=YES`

## R1 suite preserved exactly

Canonical R1 runner:

`SIGMA_PROFESSOR/artifacts/RUN_SIGMA_LANG_02A_NATIVE_OPERATOR_SCOPE_BINDING_PREFLIGHT.sh`

`R1_RUNNER_SHA256=720148bb4d22acb23e47118139095b4e73816491029ea2555448641779fc5bc4`

`R1_RUNNER_GIT_BLOB=269b7cc0f44f2d442cabd9ae079f0bad97efd69a`

`R1_ORACLE_CASES_CHANGED=NO`

R2 equality-gates that exact R1 runner, executes its complete 20-case suite, and refuses R2 admission unless the full R1 hard-gate summary is present with:

- `TOTAL_VM_INVOCATIONS=20`
- `POST_VM_ALIGNMENT_PASS_COUNT=20`
- `POST_VM_ALIGNMENT_FAIL_COUNT=0`
- `VM_NONZERO_COUNT=0`
- `STEP_LIMIT_HIT_COUNT=0`
- `NEGATIVE_TEST=PASS`
- `RESTART_REPLAY_TEST=PASS`
- `IDENTICAL_INPUT_REPLAY=YES`
- host scope selection / operator interpretation / learning / semantic interpretation all `NO`
- `PRODUCTION_STATE_MUTATED=NO`
- `LANG_02A_PREFLIGHT=PASS`
- `ADMISSION=PASS_IN_EXACT_TESTED_PREFLIGHT_SCOPE`

Any R1 failure remains failure evidence. R2 does not weaken, replace, or reinterpret an R1 oracle.

## R2 runner

Path:

`SIGMA_PROFESSOR/artifacts/RUN_SIGMA_LANG_02A_NATIVE_OPERATOR_SCOPE_BINDING_PREFLIGHT_R2.sh`

Creation commit:

`R2_RUNNER_COMMIT=da5b5d223f4b4c5ec3fbaedf4e50617fa4ffad8d`

Exact identities:

`R2_RUNNER_GIT_BLOB=0ac4b1439efece31dd60e6a5b0c66a5c70e66a8b`

`R2_RUNNER_SHA256=cbd46fffbd4aa9bf8fb5725788b7cd814874c0aea50b52d332cdeb1d5d3b26be`

Static parse observation before commit:

`R2_RUNNER_BASH_N=PASS`

This static parse observation is not locked SIGMA runtime evidence.

## Additional R2 dynamic evidence

After the exact R1 suite compiles and exposes its bytecode identity, R2 adds one extra fresh-VM case with high-entropy IDs generated after compilation.

The dynamic case contains:

- one operator;
- one wide enclosing scope;
- one narrower enclosing scope;
- one disjoint scope;
- runtime-generated opaque identifiers and coordinates.

The post-VM oracle requires native SIGMA to select the dynamically generated narrower enclosing scope.

R2 also requires:

`DYNAMIC_INPUT_PRESENT_AT_COMPILE_TIME=NO`

`UNSEEN_HIGH_ENTROPY_TOKEN_LEAK_COUNT_IN_SOURCE_OR_BYTECODE=0`

`SOURCE_UNCHANGED_AFTER_DYNAMIC_TEST=YES`

`BYTECODE_UNCHANGED_AFTER_DYNAMIC_TEST=YES`

The host creates only structural fixtures. It does not select a scope for SIGMA. The dynamic expected value is checked only after raw VM stdout/stderr are preserved.

Planned aggregate:

`R1_TOTAL_VM_INVOCATIONS=20`

`R2_ADDITIONAL_DYNAMIC_VM_INVOCATIONS=1`

`R2_TOTAL_VM_INVOCATIONS=21`

## Cognition ownership

Keep mandatory:

`SIGMA_EXECUTION_ENGINE=LOCKED_SIGMA_VM_ONLY`

`ACTIVE_CAPABILITY_IMPLEMENTATION=NATIVE_SIGMA_ONLY`

`HOST_SCOPE_SELECTION=NO`

`HOST_OPERATOR_INTERPRETATION=NO`

`HOST_LEARNING=NO`

`HOST_SEMANTIC_INTERPRETATION=NO`

`HOST_SEMANTIC_SUBSTITUTION=NO`

`HOST_POST_VM_TEST_ORACLE_ONLY=YES`

`PRODUCTION_STATE_MUTATED=NO`

## Claim ceiling

Even if all 21 invocations pass, R2 may admit only bounded native structural operator-to-scope binding in the exact tested preflight scope.

Keep:

`NUMERIC_TEXT_GENERAL_VALIDATION=NOT_PROVEN`

`SURFACE_NEGATION_RECOGNITION=NOT_PROVEN`

`LOGICAL_NEGATION=NOT_PROVEN`

`PROPOSITION_TRUTH=NOT_PROVEN`

`SEMANTIC_SCOPE=NOT_PROVEN`

`SEMANTIC_UNDERSTANDING=NOT_PROVEN`

`HUMAN_LANGUAGE_UNDERSTANDING=NOT_PROVEN`

`GENERAL_AUTONOMOUS_REASONING=NOT_PROVEN`

`SIGMA_AUTONOMOUS_LEARNING=NOT_PROVEN`

No ultimate-target PASS flag is granted by source presence, Git commit, static shell parse, compiler success alone, or a partial runtime result.

## Current evidence state

At checkpoint creation:

`P1_NATIVE_SOURCE=READY_UNCHANGED`

`P1_R2_RUNNER=SOURCE_READY`

`P1_DEPENDENCY_LOCK_DESIGN=READY`

`P1_LOCKED_SIGMAC_COMPILE=NOT_RUN_FOR_R2`

`P1_R2_TOTAL_VM_INVOCATIONS_OBSERVED=0`

`P1_R2_RUNTIME_PROOF=NOT_RUN`

`P1_R2_ADMISSION=NOT_RUN`

`PRODUCTION_BINDING=NO`

## Exact next action

On the intended Termux host, check out this candidate branch and run:

```sh
cd /data/data/com.termux/files/home/SIGMA/sigma-freedom-write

git fetch origin candidate/p1-lang02a-r2-20260912
git checkout candidate/p1-lang02a-r2-20260912

bash SIGMA_PROFESSOR/artifacts/RUN_SIGMA_LANG_02A_NATIVE_OPERATOR_SCOPE_BINDING_PREFLIGHT_R2.sh
```

Preserve the complete transcript regardless of PASS or FAIL.

A future admission checkpoint may set:

`P1_R2_ADMISSION=PASS_IN_EXACT_TESTED_R2_PREFLIGHT_SCOPE`

only if all dependency, R1, dynamic, identity-freeze, host-boundary, VM-return-code, and step-limit gates pass on the locked runtime.
