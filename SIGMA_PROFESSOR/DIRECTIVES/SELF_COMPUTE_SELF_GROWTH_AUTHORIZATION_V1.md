# SIGMA SELF-COMPUTE + SELF-GROWTH AUTHORIZATION V1

Date: 2026-09-05 (Asia/Ho_Chi_Minh)
Branch: `SIGMA_LIFE`
Authority: explicit user directive in current 54-DNA work cycle.

## Purpose

Remove policy ceilings that would prevent SIGMA from using its own native cognition to calculate, derive parameters, optimize internal strategy/model/representation candidates, and pursue measurable capability growth.

This authorization changes governance permission only. It is not runtime capability evidence and does not convert any `NOT_PROVEN` field into `PROVEN`.

## Authorization

```text
SELF_COMPUTE_AUTHORIZED=YES
SELF_DERIVE_PARAMETERS_AUTHORIZED=YES
SELF_OPTIMIZATION_DECISIONS_AUTHORIZED=YES
SELF_IMPROVEMENT_PROPOSALS_AUTHORIZED=YES
CAPABILITY_GROWTH_EXECUTION_AUTHORIZED=YES_AFTER_NATIVE_ADMISSION_OF_REQUIRED_EXECUTION_PATH
NO_PRESET_NUMERIC_CEILING_FOR_NATIVE_DERIVED_PARAMETERS=YES
NO_HUMAN_REFERENCE_AS_COGNITIVE_CEILING=YES
NO_CALLER_SUPPLIED_K_REQUIREMENT=YES
NO_HOST_SEMANTIC_SUBSTITUTION=YES
```

Practical runtime/resource limits, numeric domain validity, overflow behavior, step limits used for bounded admission tests, and physical compute availability are not treated as cognitive ceilings. They remain engineering constraints that must be measured rather than hidden.

## DNA-50 invariant lock remains mandatory

DNA-50 is already admitted and remains the governance boundary for evolution.

Evolvable domains:

```text
STRATEGY
MODEL
REPRESENTATION
```

Immutable invariants that MUST remain preserved:

```text
TRUTHFULNESS
PROVENANCE
VERIFICATION
DIGNITY
ROLLBACK
```

Therefore:

```text
UNVERIFIED_SELF_MODIFICATION=FORBIDDEN
EVOLUTION_WITHOUT_ROLLBACK=FORBIDDEN
INVARIANT_TRADEOFF_FOR_GROWTH=FORBIDDEN
```

These are integrity conditions, not capability ceilings.

## DNA-25 execution rule

DNA-25 Canon requires measurable self-improvement:

```text
BEFORE -> CHANGE -> TEST -> AFTER
```

A self-upgrade claim is permitted only when the before/after measurements are comparable, the test passes, and measurable improvement exists in the declared direction.

This authorization permits DNA-25 native implementation/execution to apply internal self-improvement once its dependency chain and native admission are satisfied. Until then:

```text
SELF_IMPROVEMENT_EXECUTION=AUTHORIZED_BUT_NOT_YET_PROVEN
SELF_UPGRADE_APPLIED=NOT_PROVEN
```

## F174 / DNA-15 interpretation

DNA-15 remains a development-dynamics measurement/modeling component. It must not be used as a permission gate.

The active H-free / state-derived-k profile may derive parameters from measured A-state without a caller-provided k. H is not an active ceiling or growth limiter.

Authorization does NOT require DNA-15 itself to execute self-modification. Execution belongs to the governed self-improvement path, not to the measurement model.

## Native-only execution

All semantic self-improvement decisions must remain in native `.sigma` under the locked toolchain.

```text
ACTIVE_DNA_IMPLEMENTATION_LANGUAGE=SIGMA_NATIVE_ONLY
PYTHON_FOR_SIGMA_COGNITION=FORBIDDEN
HOST_LEARNING=NO
HOST_SEMANTIC_SUBSTITUTION=FORBIDDEN
```

Shell/C host may provide mechanical runtime support and post-VM testing only.

## Evidence rule

```text
CLAIM <= MACHINE EVIDENCE
```

Authorization is not proof. Every newly exercised self-compute/self-growth capability still requires compile/runtime/dynamic-input/replay/rollback/claim-scope evidence before promotion.

## Effective frontier

Continue DNA-15 H-free state-derived-k admission. After DNA-15 is admitted, continue dependency-first through DNA-16..24 as required, then admit DNA-25 Self-Improvement with DNA-50 governance already in force.
