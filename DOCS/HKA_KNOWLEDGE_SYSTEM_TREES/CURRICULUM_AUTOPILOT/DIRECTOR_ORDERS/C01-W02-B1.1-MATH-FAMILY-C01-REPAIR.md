# DIRECTOR CORRECTION ORDER — C01-W02-B1.1-MATH-FAMILY-C01

Status: `REVIEW_REQUIRED`
Stage: `CURRICULUM`
Scope: `B1.1-C01 — Logic, tập hợp và chứng minh`
Director decision: C01 is NOT accepted for lock. C02 remains LOCKED.

## Purpose

Repair only the academic closure gaps found in Director audit. Do not re-author the scope, renumber accepted IDs, expand into C02, or create any Lesson Registry/prompt/image/R2/website artifact.

## Required repairs

### R1 — Propositional-logic syntax claim closure

Node: `HKA-B1-1-C01-N001`

Current D1/D2 objectives require explicit understanding of propositional variables, well-formed formulas/connectives and truth-functional classification. Add the minimum atomic sourced claim(s) necessary so these learning objectives do not depend on unstated academic content.

At minimum, the record set must explicitly support the proposition that classical propositional formulas are recursively formed from propositional variables using logical connectives, with syntax kept distinct from semantic valuation/truth conditions.

Preserve existing claim IDs. Append new claim ID(s) only.

### R2 — Set-theory axiomatic-existence claim closure

Node: `HKA-B1-1-C01-N003`

Current D3 objective requires use of standard axiomatic principles to justify set equality/existence, while the current claim set does not explicitly close the axiomatic existence basis sufficiently for that objective.

Add the minimum atomic sourced claim(s) necessary to explain the role of restricted separation/set-existence axioms in ZF/ZFC and the difference from unrestricted comprehension. Do not attempt to turn this node into a full course on axiomatic set theory; add only what is required for the existing D3/D4 learning meaning.

Preserve existing claim IDs. Append new claim ID(s) only.

### R3 — Decidability distinction closure

Node: `HKA-B1-1-C01-N008`

Current D2 objective explicitly requires the learner to distinguish `completeness`, `incompleteness`, `consistency`, and `decidability`, but the current claim set does not contain a dedicated sourced proposition establishing the meaning/boundary of decidability.

Add the minimum atomic sourced claim(s) needed to define the decision-property distinction at the metatheory level. Detailed computability, automata, Church/Turing machinery, complexity, and limits of computation remain owned by `B1.5-C02`; preserve R04 and use cross-link/prerequisite language rather than importing that curriculum into C01.

Preserve existing claim IDs. Append new claim ID(s) only.

### R4 — Immutable source pinning before acceptance

The two current Open Logic Project source records use moving website/build locators with an access date. Before Director acceptance, claims relying on Open Logic Project material must be pinned to an immutable scholarly locator where practicable: exact repository commit/tag/release, versioned publication, archived edition, DOI/ISBN/edition, or another immutable equivalent.

The correct Open Logic Project source repository is `OpenLogicProject/OpenLogic`; do not confuse it with unrelated repositories with similar names.

If a source record's normalized identity changes, mint the deterministic replacement `SOURCE_ID` according to `B1_ID_AND_RECORD_STANDARD.md`, update claim references, and preserve provenance/version history. Do not silently mutate source identity while retaining an invalid deterministic ID.

### R5 — Claim-to-objective closure audit

After R1–R4, audit all 32 learning objectives against the final claim/source set.

For every learning objective, answer machine-readably: `SUPPORTED_BY_CLAIMS = true|false` and list supporting claim IDs. No objective may require the future Lesson Registry to invent new academic propositions after `ACADEMIC_LOCKED`.

If another closure gap is found, append the minimum academically necessary claim/source record now. Do not broaden scope merely to increase record count.

## Mandatory re-audit

Re-run all existing PASS gates plus:

1. `claim_to_learning_objective_closure = PASS`
2. `immutable_source_pin = PASS`
3. `director_identified_gaps = 0`
4. `R04 ownership boundary = PASS`
5. `semantic_duplicate_scan = PASS`
6. `prerequisite graph = PASS`
7. `stage boundary = PASS`

The re-audit must inspect committed files, not draft memory.

## Required terminal status

Do NOT return `PASS` unless every repair above is complete.

Allowed outcomes:
- `PASS`
- `REVIEW_REQUIRED`
- `BLOCKED_INPUT`
- `BLOCKED_CONTRADICTION`

On PASS, `next_action` remains exactly:
`C01-W02-B1.1-MATH-FAMILY-C02`

Until Director independently accepts the repaired result, C02 remains LOCKED.
