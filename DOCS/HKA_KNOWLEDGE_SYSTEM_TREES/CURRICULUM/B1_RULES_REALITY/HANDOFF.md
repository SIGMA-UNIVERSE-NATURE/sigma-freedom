# C01-W01-B1-ARCHITECTURE — Handoff

Stage: `CURRICULUM`  
Execution branch: `hka-tree/c01-w01-b1-architecture`  
Execution baseline: `02ff47d64fd3b3b03d1fa2ae70d773afb071995e`  
Canonical tree: `DOCS/HKA_KNOWLEDGE_SYSTEM_TREES.md@fc799bf1104ab6352710e1801777a971b5179995`

This handoff is only a recovery index. Authoritative rules, scope, ownership, partitioning and duplicate controls are in the sibling artifacts and `RESULT.json`; nothing essential is defined only here.

## Recovery order

A replacement/successor must read, in order:

1. Canonical pipeline/state/recovery/window controls from `hka-tree/curriculum-master`.
2. This window's locked `GPT_EXECUTION_PROMPT.md` and `WINDOW_CONTRACT.md`.
3. `B1_SCOPE_MAP.json` for stable scopes/topics/owners/prerequisites/cross-links.
4. `B1_COVERAGE_MATRIX.md` for exhaustive topic reconciliation.
5. `B1_ID_AND_RECORD_STANDARD.md` for curriculum record IDs and minimum HKA node requirements.
6. `B1_AUTHORING_SEQUENCE.md` for bounded child execution, exact predecessor pinning and restart behavior.
7. `B1_DUPLICATE_CONTROL.md` for semantic duplicate/overlap dispositions.
8. `RESULT.json` for the terminal checkpoint and permitted next action.

Do not use chat history as project state and do not renumber accepted IDs.

## Architecture checkpoint

- Five canonical B1 subbranches are preserved.
- 68 stable primary authoring scopes account for 348 canonical topic leaves.
- Every canonical topic has exactly one primary owner; orphan and multi-primary counts are zero.
- 52 bounded child windows are defined; the largest child has 10 canonical topics.
- The scope prerequisite graph is acyclic.
- Three exact repeated topic labels and 15 semantic/cross-domain risk cases have explicit controls.
- D1–D4 remains independent of age/presentation.
- No Lesson Registry, visual/image prompt, image, R2, delivery or website artifact is authored here.
- No B1/global `ACADEMIC_LOCKED` state is asserted by this window.

## Successor rule

Only a committed `RESULT.json` with `status=PASS` unlocks continuation. If PASS is present, the sole next registered family is `C01-W02-B1.1-MATH-FAMILY`, which must begin with bounded child `C01-W02-B1.1-MATH-FAMILY-C01` and follow `B1_AUTHORING_SEQUENCE.md`.

If PASS is absent, treat this window as incomplete and resume only unfinished architecture output from GitHub.
