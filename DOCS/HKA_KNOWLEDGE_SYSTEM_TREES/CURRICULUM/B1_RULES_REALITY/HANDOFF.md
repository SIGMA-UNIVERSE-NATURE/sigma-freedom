# C01-W01-B1-ARCHITECTURE — Handoff

Stage: `CURRICULUM`  
Execution branch: `hka-tree/c01-w01-b1-architecture`  
Execution baseline: `02ff47d64fd3b3b03d1fa2ae70d773afb071995e`  
Canonical tree: `DOCS/HKA_KNOWLEDGE_SYSTEM_TREES.md@fc799bf1104ab6352710e1801777a971b5179995`

This handoff is only a concise recovery index. Machine-critical status lives in the mandatory durable status folder and canonical sibling artifacts; nothing essential is defined only here or in chat.

## Recovery order

A replacement/successor must bootstrap from GitHub in this order:

1. `HKA_PIPELINE_CANONICAL.json` and `HKA_CURRICULUM_STATE.json` on `hka-tree/curriculum-master`.
2. `STATUS_REPORTS/C01-W01-B1-ARCHITECTURE/STATUS.json`.
3. `STATUS_REPORTS/C01-W01-B1-ARCHITECTURE/REPORT.md`.
4. The latest append-only checkpoint named by `STATUS.json`.
5. The exact committed output paths/commits referenced by the durable status.
6. This window's current `GPT_EXECUTION_PROMPT.md`, `WINDOW_CONTRACT.md`, recovery/status standards and registry.
7. `B1_SCOPE_MAP.json`, `B1_COVERAGE_MATRIX.md`, `B1_ID_AND_RECORD_STANDARD.md`, `B1_AUTHORING_SEQUENCE.md`, `B1_DUPLICATE_CONTROL.md`.
8. `RESULT.json` for the terminal window decision and permitted successor.

If chat differs from GitHub durable state, GitHub wins. Do not renumber accepted IDs.

## Architecture checkpoint

- Five canonical B1 subbranches are preserved.
- 68 stable primary authoring scopes account for all 348 canonical topic leaves.
- Every canonical topic has exactly one primary owner; orphan and multi-primary counts are zero.
- 52 bounded child windows are defined; the largest child has 10 canonical topics.
- The 68-scope prerequisite graph is acyclic and contains no unknown scope prerequisite.
- Three exact repeated topic labels (`Dao động`, `Mã sửa lỗi`, `Entropy`) and 15 semantic/cross-domain risk cases have explicit control paths.
- All eight mandatory cross-domain nodes can receive B1 secondary links without transferring primary ownership.
- D1–D4 remains independent of age/presentation.
- No Lesson Registry record, visual/image prompt, image, R2 object, delivery artifact or website update is authored here.
- No B1/global `ACADEMIC_LOCKED` state is asserted by this window.
- Concurrent governance updates `7ee6efa385909139fb4718bca5eb72ecd8f73732` and `f52ddcb7fd7c6c821fede8af452c80efcc8ddead` were adopted; they require durable status reporting and do not alter the curriculum-only boundary.

## Locked ownership / restart rules

`B1_SCOPE_MAP.json` is authoritative for stable scope/topic ownership. `B1_AUTHORING_SEQUENCE.md` is authoritative for bounded child IDs, order, output directories and accepted-predecessor selection. `B1_ID_AND_RECORD_STANDARD.md` is authoritative for Node/Claim/Source/Learning Objective and non-registry future lesson-slot reference IDs. `B1_DUPLICATE_CONTROL.md` is authoritative for `R01`–`R15` dispositions.

A family/child is accepted only when its durable `STATUS.json` and its `RESULT.json` both say `PASS`, with the required checkpoint/report present. A replacement resumes the first unfinished child and preserves all accepted IDs/artifacts.

## Successor rule

This architecture window unlocks nothing unless its own final durable `STATUS.json.status=PASS`, `RESULT.json.status=PASS`, current `REPORT.md` records PASS, and required checkpoint/output files are present.

When those conditions hold, the sole next registered family is `C01-W02-B1.1-MATH-FAMILY`. That controller must begin only with bounded child `C01-W02-B1.1-MATH-FAMILY-C01` and pin the accepted architecture predecessor exactly as specified in `B1_AUTHORING_SEQUENCE.md`.

If those PASS conditions do not all hold, treat this window as incomplete and resume only the remaining architecture work from GitHub.
