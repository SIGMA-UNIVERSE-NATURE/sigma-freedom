# Checkpoint — Provisional Truth R1H2 PASS and Multi-Scope R1

Date: 2026-09-09 +07.

## Scoped Provisional Epistemic Truth R1H2 — runtime truth

Target core SHA256: `bf468c564451839d3be9b22243039fe71ceb87b766a165d996f4be055f7cbbf1`.

Oppo bytecode SHA256: `569411458b1bff9c0c9894fd95374a87db6e6e5c04030dc8e8900e1cb0d38ea2`.

Admission PASS and independent blind PASS, both `RC=0`.

PASS includes:

- parent Source-Consistency Revision R2 regression;
- `UNRESOLVED` state;
- unopposed clean distinct-source `PROVISIONAL_A`;
- self-inconsistent source excluded from truth contest;
- clean counter-evidence creates `CONTESTED`;
- evidence-backed competing-configuration marker in the exact native relation-discrimination scope;
- revision policy `HELD` cannot override `CONTESTED` truth-state;
- retroactive source-authority retraction can move truth to `PROVISIONAL_B`;
- later clean counter-evidence reopens `CONTESTED`;
- invalid/no-stance evidence does not perturb truth-state;
- restart persistence;
- `NATIVE_SCOPED_PROVISIONAL_EPISTEMIC_TRUTH=PASS` in the exact native two-candidate/source-consistency scope.

Keep `BROAD_SEMANTIC_SUPPORT_CONFLICT_TRUTH=FAIL` and `WHOLE_WORK_UNDERSTANDING=FAIL`. Those claims are broader than this blind.

Production artifact hash freeze PASS and production mutation NO. The blind observed production PID set `5796,23663` after starting from `23663`; process-set variation is not used as the immutable artifact fingerprint and is left to the synchronization/operations gate to observe.

## Exact next bottleneck

Static audit of the admitted truth core shows a single active global `native_scoped_revision_*` / `native_scoped_provisional_truth_*` slot. One relation can be reasoned about correctly, but multiple relations inside the same work cannot coexist without explicit archiving/switching support. This is a representation bottleneck before whole-work understanding.

## Prepared Gate A candidate — Multi-Scope Epistemic Work Model R1

Candidate ID: `M5_NATIVE_MULTI_SCOPE_EPISTEMIC_WORK_MODEL_R1`.

Core SHA256: `c0777f1ae348caceeed007e5762e8b8c70ac20b87b6a2592e63e921be8455796`.

Admission evaluator SHA256: `74df3a624bfa28cca35f5f823003e15d275b6b49b85f29d74aa9087b7366e893`.

Independent blind evaluator SHA256: `bbcd5d2cbee8d8015db88698148a2f405ee15c41851a1a19eddab6e84f1b0765`.

Ladder runner SHA256: `485db2479d80a7804e371045959bc99fa65de026b1b4d119d63e22c1224811b8`.

Combined ladder bundle SHA256: `596a0d450415a1e539542235b585995ddd4673ef0fd8a9457dd37922323832b7`.

Design:

- bounded native bank of up to eight epistemic scopes per work;
- scope identity is mechanical; candidate A/B still originate from native relation-discrimination gaps;
- active scope can be archived, later reactivated, revised with new evidence, and rearchived;
- three same-work scopes may hold different truth states simultaneously;
- later learning/revision of one scope must leave other archived scope bytes unchanged;
- whole-work epistemic-model recall returns all archived scopes for the requested work;
- work-scope isolation and restart are blind gates;
- parent Provisional Truth R1H2 regression is mandatory.

Static checks: admission/blind core byte-identical; brace balance clean; host-op set remains only `read_text`, `write_text`, `str_len`, `str_replace`, `str_split`, `list_len`, `list_get`; forbidden LEFT/RIGHT/previous-next/grammar-role markers absent; Bash syntax PASS; ZIP integrity PASS.

If PASS, advance only `NATIVE_MULTI_SCOPE_EPISTEMIC_WORK_MODEL=PASS`. Do not call it whole-work understanding.

Next after that: native cross-scope dependency induction and a query that requires evidence from at least two distant scopes in one work.