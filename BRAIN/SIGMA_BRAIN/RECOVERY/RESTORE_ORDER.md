# SIGMA_BRAIN RESTORE ORDER

Date: 2026-08-19

Use this order after loss of OPPO or when a successor window needs to reconstruct the development state.

## 0. Do not use chat memory as source of truth

Repository: `SIGMA-UNIVERSE-NATURE/sigma-freedom`
Branch: `SIGMA_BRAIN`

Clone/fetch the branch first. Work in a clean recovery directory.

## 1. Identity and state

Read:

1. `BRAIN/SIGMA_BRAIN/RECOVERY/README_FIRST.md`
2. `BRAIN/SIGMA_BRAIN/MASTER_INDEX.md`
3. `BRAIN/SIGMA_BRAIN/CURRENT_STATE.json`
4. `BRAIN/SIGMA_BRAIN/BRANCH_AUDIT_20260819.json`
5. `BRAIN/SIGMA_BRAIN/FINGERPRINTS/SIGMA_BRAIN_MASTER_FINGERPRINT_20260819.json`
6. `BRAIN/SIGMA_BRAIN/RECOVERY/SIGMA_BRAIN_RECOVERY_MANIFEST_20260819.json`

## 2. Foundation

Read Foundation V7 handoff and verify immutable hashes. Do not modify V7 in place.

Then read persisted Phase-2 hardening candidate. It is post-foundation candidate evidence, not canonical V7 mutation.

## 3. SIGMA-PSI language identity

Read native-language constitution and language-evolution hinge before interpreting C/Python behavior.

Rule: SIGMA-PSI already exists; external languages are translation/substrate references, not permanent semantic authority.

## 4. Reconstruct the accepted development path

In order:

1. `SIGMA_SEED_CHAU_0001` — PASS_WITH_DEFINED_SCOPE.
2. `SIGMA_CREATES_SIGMA_0002` — independent accepted bounded self-construction.
3. `SIGMA_CREATES_SIGMA_0003` — exact self-assembly PASS; Native Brain runtime HOLD at MNEW.
4. Frozen `SIGMA_NATIVE_BRAIN_54X512_V0_1` — preserve identity used by 0003.
5. VM actual-ABI evidence and VM experiment ledger/map/history.
6. Tam Van 0004 v0.1/v0.2 accepted defined scope; v0.3 and free-form gates remain only as recorded evidence.
7. Dictionary/spec/reference snapshot.

## 5. VM recovery semantics

Do not stop at the older v0.6 ledger. Historical VM records show later progress:

- v0.8 FLOAT = PASS_WITH_TESTED_SCOPE.
- v0.9 FLOORDIV exact parity = PASS_WITH_TESTED_SCOPE.
- v0.9 FLOAT regression = PASS.
- v0.9 FUNCTION/LOCAL regression = PASS.
- v0.9 overall = HOLD at the recorded point because binary exact-byte comparison + unary + IF/WHILE regression gates remained open.

Inherit these states exactly. Do not rerun earlier VM gates unless source/hash or a newly defined edge case requires it.

## 6. Historical archive

Everything under `BRAIN/SIGMA_BRAIN/ARCHIVE/` is evidence/history, not automatic active architecture.

It contains serious but superseded/partial work so successor teams can understand why later decisions were made and avoid repeating discarded paths.

Never promote archive content merely because it exists.

## 7. Restore OPPO executable substrate

Before execution, locate exact OPPO artifacts by SHA-256. Follow:

`BRAIN/SIGMA_BRAIN/RECOVERY/IMPORT_OPPO_FINGERPRINTED_ARTIFACTS.md`

Current six required exact fingerprints are in the recovery manifest.

If their content is not yet under `BRAIN/SIGMA_BRAIN/OPPO_SNAPSHOT/20260819/`, full executable restore is HOLD. Do not invent or regenerate an accepted source file from GPT recollection.

## 8. Platform rule

Source/semantic core should be restored identically. Native binaries are platform-specific.

Do not copy Android/aarch64 executable binaries into HP/Windows/x86_64 or vice versa. Rebuild platform adapters/binaries from exact source and verify behavior.

## 9. Continue development

Continue only the next unproven gate:

`one real input -> observe real SIGMA output -> repair/teach/redefine under SIGMA-PSI if wrong -> machine evidence -> independent evaluation when required -> update dictionary/spec -> save off-device -> verify -> next input`

Failure is evidence. Missing evidence is HOLD.
