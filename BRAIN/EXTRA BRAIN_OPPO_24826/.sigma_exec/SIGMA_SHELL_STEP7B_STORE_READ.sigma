#SIGMAUNIVERSE_LANGUAGE[DOMAIN=SIGMA.SHELL][VERSION=0.5]

DEF H(op, a, b, c) {
    RETURN host(op, a, b, c);
}

⟡(Σ.SHELL) {

    ⚡ OBSERVATION:
        H("input", "", NULL, NULL);

    ⚡ WRITE_RESULT:
        H("write_text", "sigma_shell_observation.state", OBSERVATION, NULL);

    ⚡ READ_RESULT:
        H("read_text", "sigma_shell_observation.state", NULL, NULL);


    ⋈ ACTION {

        ⚡ print("============================================");
        ⚡ print("Σ.SHELL — STEP 7B STORE READ");
        ⚡ print("============================================");

        ⚡ print(READ_RESULT);

        ⚡ print("SIGMA_SHELL_STOPPED");
    }
}
