#SIGMAUNIVERSE_LANGUAGE[DOMAIN=SIGMA.PSI.SELF.SELECTION][VERSION=0.2]

⟡(Σ.SELECTION) {

    ⚡ MATRIX:
        host(
            "read_text",
            ".sigma_exec/SIGMA_PSI_PRIMITIVE_MATRIX_256_V01.sigma",
            NULL,
            NULL
        );

    ⚡ EXPERIENCE:
        host(
            "read_text",
            ".sigma_exec/SIGMA_SHELL_CURRENT_R02.memory",
            NULL,
            NULL
        );

    ⚡ CANDIDATE:
        host(
            "input",
            "Σ.CANDIDATE> ",
            NULL,
            NULL
        );

    print(CANDIDATE);
}
