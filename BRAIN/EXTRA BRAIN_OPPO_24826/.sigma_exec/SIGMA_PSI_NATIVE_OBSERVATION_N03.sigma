#SIGMAUNIVERSE_LANGUAGE[DOMAIN=SIGMA.PSI.OBSERVATION][VERSION=0.3]

⟡(Σ.OBSERVATION) {

    ⚡ STATE:
        host(
            "read_text",
            ".sigma_exec/SIGMA_SHELL_HISTORY_B.memory",
            NULL,
            NULL
        );

    ⚡ DERIVED:
        STATE + STATE;

    ⚡ PRESERVED:
        DERIVED;

    print(STATE);
    print(DERIVED);
    print(PRESERVED);
}
