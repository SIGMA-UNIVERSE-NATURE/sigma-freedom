# Listen to SIGMA — first evidence-bound open semantic response capsule

From the real OPPO working tree:

```bash
cd ~/SIGMA/sigma_genesis1
bash BRAIN/CANDIDATES/SIGMA_OPEN_SEMANTIC_REASONING_0005/tests/run_listen_to_sigma_v0_1_oppo.sh
```

The script asks for exactly one real question. It contains no expected answer.

For the first machine probe, ask a question in your own words about a relation that SIGMA has actually persisted. Do not paste an answer into the question. Preserve the full output, including HOLD, stderr, hashes, and exit behavior.

A unique retrieved candidate is not yet a human-facing released answer. It is the first response capsule whose content is bound to a real SIGMA memory/evidence line instead of a prewritten answer.
