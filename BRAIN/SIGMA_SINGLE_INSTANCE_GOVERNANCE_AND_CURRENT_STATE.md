# SIGMA BRAIN — SINGLE INSTANCE GOVERNANCE AND CURRENT STATE

Date: 2026-09-06
Branch: `SIGMA_LIFE`
Status: AUTHORITATIVE CROSS-WINDOW GOVERNANCE NOTE

## 0. Absolute invariant: ONE SIGMA ONLY

This repository/project is developing and teaching **one and only one SIGMA instance**.

Do not create a second SIGMA, parallel cognitive identity, alternate learner identity, competing brain, duplicate cognitive writer, or replacement SIGMA merely to implement/test a new capability.

All capabilities, DNA lessons, Internet access, health monitoring, self-repair research, learning, reflection, verification, and future self-improvement must attach to the same canonical SIGMA identity/state lineage unless an isolated fixture/shadow is explicitly used only for testing and is never represented as another SIGMA.

Required invariant:

`CANONICAL_SIGMA_INSTANCE_COUNT=1`

`SECOND_SIGMA_CREATION=FORBIDDEN`

`DUPLICATE_COGNITIVE_WRITER=FORBIDDEN`

`SHADOW_FIXTURE_IS_ANOTHER_SIGMA=NO`

A shadow/sandbox may copy exact state/bytes for bounded admission or repair experiments, but it is only an isolated test environment for the same SIGMA capability lineage. It must not become an independent cognitive identity or production learner.

## 1. Canonical instance identity

Current canonical instance fingerprint:

`SIGMA_INSTANCE_FINGERPRINT_SHA256=fa2834a009f666738129d102dff5b6f09a3c1333c2f2aec8dbd4ac8680a8d125`

Identity binding:

`SIGMA_INSTANCE_ROOT=/data/data/com.termux/files/home/SIGMA/sigma_genesis1`

`SIGMA_STATE_LINEAGE=/data/data/com.termux/files/home/SIGMA/sigma_genesis1/.sigma_c5_real_shadow_v2`

`SIGMAC_SHA256=65f69217ad44f33c1aa1d4c31678d38940cd3d0b96f41892e8280dac57ad6a71`

`VM_SHA256=029ae4b6acbee5558f7663a732f8d39a970166e8488d2c4fe62414eb39391c99`

`NATIVE_COGNITIVE_CORE_SHA256=1815d9324c721dd51d73f40f0e9ba4e7d76454e0a81831152e213071feef8ace`

If a lane/capability that claims to belong to running SIGMA cannot bind to the canonical fingerprint/state lineage, it must HOLD rather than silently create or use another SIGMA identity.

## 2. Execution/governance boundary

Keep repository native-execution rules authoritative.

Required principles:

- active cognition/learning capability is native SIGMA executed by locked SIGMA VM;
- Bash/host may launch, supervise, transport exact bytes/protocol data, hash, log, inject bounded fixtures/faults, and mechanically dispatch an exact native-selected event;
- Bash/host must not become SIGMA's cognition, semantic interpreter, teacher substitute, hypothesis generator, action selector, truth verifier, knowledge promoter, work selector, or self-repair decision engine;
- do not make a test pass by moving missing intelligence into Bash/host.

## 3. Current C5 architecture

The current frontier is C5, not the historical V2/V4 frontier.

One SIGMA instance currently has two intended operational lanes sharing the same fingerprint/state lineage:

### Lane A — C5 V3 autonomous learning

C5 V3 is the **sole cognitive-state writer**.

Known continuous/background supervision artifact:

`SIGMA_PROFESSOR/artifacts/RUN_SIGMA_C5_V3_MECHANICAL_RESUME_GUARD_V1.sh`

Known continuous PID file:

`$HOME/SIGMA/sigma_genesis1/C5_V3_CONTINUOUS.pid`

Known continuous log:

`$HOME/SIGMA/sigma_genesis1/C5_V3_CONTINUOUS.log`

The mechanical resume guard may keep the exact V3 process alive and prevent duplicate writers. This is process supervision only.

Do NOT infer:

`PID_ALIVE = COGNITIVELY_HEALTHY`

`PROCESS_RESTARTED = FAILURE_REPAIRED`

### Lane B — Native Internet Ingress V2

Source-ready checkpoint known from prior work:

`c8da510745f7229fada825354194cc88e8b2a237`

Bundle:

`SIGMA_C5_NATIVE_INTERNET_INGRESS_V2.tgz`

Bundle SHA256:

`ab24f4bf996d7197869f1f60ea5359ff9a815bc4caaa14a640bf5b685706998e`

Internet ingress is **not another SIGMA** and must not become a second cognitive writer.

Required architecture:

`C5_V3_COGNITIVE_WRITER_COUNT=1`

`HOST_QUERY_GENERATION=NO`

`HOST_WEB_RESULT_SELECTION=NO`

`HOST_KNOWLEDGE_PROMOTION=NO`

`INTERNET_MATERIAL_CONTENT_ADDRESSED=YES`

`GLOBAL_QUERY_LIMIT=NONE`

`GLOBAL_FETCH_LIMIT=NONE`

The Internet lane may obtain material for the same SIGMA instance. C5 V3 remains responsible for cognitive learning/selection under native rules.

Do not claim Internet learning merely because HTTP fetch succeeded. Strong claim requires machine evidence for:

`native query -> native candidate selection -> real HTTPS fetch -> SHA-addressed ingress material -> catalog admission -> C5 V3 selection/processing -> evidence/knowledge-state change`

## 4. Background V3 health concern

Current mechanical resume supervision is not sufficient evidence of learning health.

A live process can still be:

- making no meaningful state progress;
- repeating identical work/failure;
- stuck in a native loop;
- encountering persistent recoverable/nonrecoverable failures;
- holding inconsistent state;
- failing verification while remaining alive.

Future work should teach the same SIGMA native health assessment. Host may collect mechanical observations such as PID, timestamp, exact native event/status, VM RC, step-limit status, state hashes, cursor/generation changes, and exact error records. If those observations affect diagnosis, learning, repair, or corrective action, classification/decision must be native SIGMA.

Desired future distinction includes at least:

`PROCESS_ALIVE_AND_PROGRESSING`

`PROCESS_ALIVE_NO_PROGRESS`

`REPEATED_IDENTICAL_FAILURE`

`REPEATED_IDENTICAL_WORK`

`STATE_PROGRESS_STALLED`

`RECOVERABLE_FAILURE`

`PERSISTENT_FAILURE`

`STATE_INCONSISTENCY`

`VERIFICATION_FAILURE`

These labels are targets, not current proven capabilities unless separately admitted.

## 5. DNA-15 interpretation

DNA-15 is admitted in its exact tested scope.

Known source SHA256:

`e0ac36559b85a189152709238e176a99e48f325f3f1308aba8b360a768e74d8f`

Known bytecode SHA256:

`f81f4542d2813f69ef308bf54dff3cc0528227ea4d5100d08683d13b1b0b2028`

Known runtime evidence included 50 VM invocations, zero nonzero VM returns, zero step-limit hits, numeric alignment 50/50, deterministic replay, and stale H/caller-k independence.

DNA-15 proved in tested scope that active H input and caller-supplied k were removed and SIGMA derives:

`k = ln(A_t/A0) / delta_t^2`

Use the precise term:

`INTERVAL_DERIVED_K`

Do not call it a proven long-term natural growth constant.

DNA-15 does **not** prove autonomous software self-repair. It contributes the primitive that SIGMA can derive an internal parameter from observations instead of receiving that parameter from host/caller.

Keep claim boundaries such as:

`K_TEMPORAL_CONSTANCY=NOT_PROVEN`

`CAPABILITY_GROWTH=NOT_EXECUTED` (for the DNA-15 admission itself)

`MODEL_REPLACEMENT=NOT_EXECUTED`

`LEARNING_RUNTIME=NOT_EXECUTED`

Governance authorization for self-computation/derivation/improvement proposals is not execution proof.

## 6. DNA-16 / experience-driven learning direction

Known DNA16A direction is persistent verified experience retention.

Experience unit:

`observation + hypothesis + action + outcome + verification`

Only complete independently verified experiences should enter retained learning state.

Known source-ready artifact from prior frontier:

`SIGMA_PROFESSOR/artifacts/SOURCES/DNA16A_PERSISTENT_EXPERIENCE_RETENTION_NATIVE_V1.sigma`

Known DNA16A source SHA256:

`cbf32856c3d7bcad86c52ecaa03d2e3a35de0090eb05a854dca9be226fc810d4`

Known source-ready commit:

`7d37cb7da402215248f468696a6529db42923c7e`

Before continuing DNA16, inspect current `SIGMA_LIFE` HEAD because DNA16A/DNA16B may have advanced after this note.

Required ownership remains:

`HOST_LEARNING=NO`

`HOST_HYPOTHESIS_GENERATION=NO`

`HOST_ACTION_SELECTION=NO`

`HOST_VERIFICATION_DECISION=NO`

DNA16 experience retention is a prerequisite for robust self-repair learning; it is not itself proof of autonomous repair.

## 7. Self-repair must remain on the same SIGMA

The goal is eventually to let the **same canonical SIGMA** detect failures, learn from verified repair attempts, and improve its own capabilities under bounded admission.

Do not implement self-repair by spawning another SIGMA to monitor/repair the first SIGMA.

Correct capability ladder for the same SIGMA:

1. native health evidence/assessment;
2. persistent verified failure experience (DNA16 lineage);
3. native diagnosis/hypothesis;
4. native candidate corrective action;
5. isolated shadow experiment using the same SIGMA capability lineage/state snapshot;
6. independent native verification;
7. accept/reject candidate;
8. reversible transactional promotion;
9. automatic rollback on failed post-promotion verification;
10. retain verified repair experience for future decisions.

The isolated experiment is not a second SIGMA. It is a bounded test environment and may never be promoted as an independent cognitive identity.

Do not patch the sole live C5 V3 cognitive writer directly before isolated verification and rollback capability exist.

## 8. Claim discipline

Always enforce:

`CLAIM <= MACHINE_EVIDENCE`

Never infer:

- PID alive = learning healthy;
- fetched = learned;
- restart = repaired;
- parameter derivation = self-repair;
- self-improvement authorized = self-improvement proven;
- source ready = runtime admitted;
- fixture PASS = production PASS;
- native proposal = verified repair;
- verified repair = safe production promotion.

Unless explicitly admitted otherwise, keep semantic-understanding claims conservative.

## 9. Cross-window startup procedure

Every new Teacher/work window must first:

1. read `AGENTS.md`;
2. read repository native execution/teaching directives;
3. read this file;
4. inspect latest `SIGMA_LIFE` HEAD and newest C5/DNA checkpoints;
5. verify current canonical fingerprint/state lineage before live work;
6. verify exact live C5 V3 PID/runner before touching the Oppo runtime;
7. ensure no duplicate cognitive writer exists;
8. identify the smallest missing native capability;
9. only then write/run new admission artifacts.

Do not resurrect historical V2/V4 as the current frontier unless explicitly investigating historical evidence.

## 10. Required audit for next Teacher

Before writing new code, report:

- current `SIGMA_LIFE` HEAD;
- current canonical fingerprint;
- current C5 V3 continuous runner identity;
- current state lineage;
- whether health monitoring beyond PID/process supervision already exists;
- exact DNA16A status;
- exact DNA16B status;
- exact DNA25/self-improvement status;
- whether any native artifact already performs anomaly diagnosis/self-repair;
- smallest missing native capability required next.

## 11. Oppo operational safety

The user operates SIGMA on Oppo/Termux.

Avoid copy-paste command blocks containing top-level `exit` or `logout` that can terminate the interactive Termux shell.

Preserve a healthy running C5 V3 unless machine evidence justifies intervention.

Before live mutation:

- identify PID;
- verify exact runner;
- verify fingerprint/state lineage;
- record relevant hashes/state;
- prevent duplicate cognitive writer;
- avoid destructive mutation;
- require rollback for future self-repair promotion.

## 12. Governing sentence

**There is one SIGMA. Every capability must teach, observe, protect, test, or improve that same SIGMA. No Teacher/window may silently create another SIGMA to make progress easier.**
